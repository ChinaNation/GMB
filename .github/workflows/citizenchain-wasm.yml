name: CitizenChain WASM

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/citizenchain-wasm.yml"
      - "citizenchain/runtime/**"
  workflow_dispatch:

concurrency:
  group: citizenchain-wasm-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  build-wasm:
    name: 编译 WASM
    runs-on: ubuntu-latest
    steps:
      - name: 拉取代码
        uses: actions/checkout@v4

      - name: 安装 Rust 工具链
        uses: dtolnay/rust-toolchain@1.94.0
        with:
          targets: wasm32v1-none
          components: rust-src

      - name: 安装系统依赖
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler libclang-dev

      - name: spec_version 自增
        run: |
          FILE=citizenchain/runtime/src/lib.rs
          OLD=$(grep -oP 'spec_version:\s*\K[0-9]+' "$FILE")
          NEW=$((OLD + 1))
          sed -i "s/spec_version: $OLD/spec_version: $NEW/" "$FILE"
          echo "spec_version: $OLD → $NEW"

      - name: 清理旧版 WASM artifact
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api repos/${{ github.repository }}/actions/artifacts \
            --jq '.artifacts[] | select(.name == "citizenchain-wasm") | .id' \
          | while read -r id; do
              echo "删除旧 artifact: $id"
              gh api -X DELETE repos/${{ github.repository }}/actions/artifacts/$id || true
            done

      - name: 删除全部旧编译产物
        working-directory: citizenchain
        run: |
          rm -rf target
          echo "已删除全部 target 目录，从零开始编译"

      - name: 编译 WASM
        working-directory: citizenchain
        env:
          WASM_BUILD_FROM_SOURCE: "1"
        run: cargo build --release -p citizenchain

      - name: 上传 WASM
        uses: actions/upload-artifact@v4
        with:
          name: citizenchain-wasm
          path: |
            citizenchain/target/release/wbuild/citizenchain/citizenchain.wasm
            citizenchain/target/release/wbuild/citizenchain/citizenchain.compact.wasm
            citizenchain/target/release/wbuild/citizenchain/citizenchain.compact.compressed.wasm
          retention-days: 30

      - name: 提交 spec_version 自增
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add citizenchain/runtime/src/lib.rs
          NEW=$(grep -oP 'spec_version:\s*\K[0-9]+' citizenchain/runtime/src/lib.rs)
          git commit -m "runtime: spec_version → $NEW [skip ci]"
          git push
