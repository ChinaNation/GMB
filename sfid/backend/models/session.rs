//! 中文注释:登录会话相关 DTO 占位文件。
//!
//! 实际的 `AdminSession` / `LoginChallenge` / `QrLoginResultRecord` / `LoginPayload`
//! 等类型当前定义在 `crate::login` 模块下,通过 `crate::login::*` 直接消费。
//! 本文件仅做语义占位,未来若把会话 DTO 从 login 抽离时再下沉至此。
