任务需求：
CPMS 所有需要输入日期的前端位置统一使用同一个日期输入组件，修复公民档案列表出生日期搜索框输入 4 位年份后不自动切到月份的问题，并清理散落的日期输入残留。

所属模块：
CPMS 前端公共组件、档案列表、档案创建、档案详情编辑、CPMS 技术文档。

完成项：
- 新增 `cpms/frontend/components/DateInput.tsx`，集中封装日期输入、默认昨日 `max`、`YYYY-MM-DD` 校验辅助函数。
- 档案列表出生日期搜索、创建档案出生日期、编辑档案出生日期全部改用 `DateInput`。
- 删除 `ArchiveCreate.tsx` 和 `ArchiveDetail.tsx` 中散落的重复日期辅助实现。
- 文档补充 CPMS 前端日期输入统一规则，明确页面不得散落直接的原生日期输入标签。

验证记录：
- `npm run build` 通过。
- `git diff --check` 通过。
- 残留扫描通过：`ArchiveList.tsx / ArchiveCreate.tsx / ArchiveDetail.tsx` 不再直接写原生日期输入，只保留 `DateInput.tsx` 内部实现。
