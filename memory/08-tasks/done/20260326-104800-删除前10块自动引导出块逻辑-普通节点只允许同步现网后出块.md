# 任务卡：删除前 10 块自动引导出块逻辑，普通节点只允许同步现网后出块

- 任务编号：20260326-104800
- 状态：in-progress
- 所属模块：citizenchain/node
- 当前负责人：Codex
- 创建时间：2026-03-26 10:48:00

## 任务需求

现网已经存在运行中的节点和高度 10 的区块链，不再需要“软件启动后自动引导前 10 个空块”的历史兼容逻辑。普通节点必须先同步现网，之后才允许参与正常出块。

## 模块边界

- 允许改动：
  - `citizenchain/node/src/service.rs`
  - `memory/01-architecture/citizenchain/CITIZENCHAIN_TECHNICAL.md`
  - 相关任务卡文档
- 不修改 runtime 规则和 nodeui 启动参数。

## 风险说明

- 删除该逻辑后，空网状态下不会再自动自举出块；这符合“必须先同步现网”的当前产品要求。
- 必须同步清理旧文档，否则后续会继续按“前 10 块自动放行”理解当前实现。

## 实施记录

- 已确认现有代码仍保留 `best_number < 10` 时放行空块的残留逻辑。
- 已确认用户要求现网节点统一遵循“先同步后出块”，不再允许自动引导前 10 块。
