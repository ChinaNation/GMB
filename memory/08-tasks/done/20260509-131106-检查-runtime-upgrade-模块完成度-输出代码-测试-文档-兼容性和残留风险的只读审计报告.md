# 任务卡：检查 runtime-upgrade 模块完成度，输出代码、测试、文档、兼容性和残留风险的只读审计报告

- 任务编号：20260509-131106
- 状态：done
- 所属模块：citizenchain
- 当前负责人：Codex
- 创建时间：2026-05-09 13:11:06

## 任务需求

检查 runtime-upgrade 模块完成度，输出代码、测试、文档、兼容性和残留风险的只读审计报告

## 必读上下文

- memory/00-vision/project-goal.md
- memory/00-vision/trust-boundary.md
- memory/01-architecture/repo-map.md
- memory/03-security/security-rules.md
- memory/07-ai/agent-rules.md
- memory/07-ai/context-loading-order.md
- <补充该模块对应技术文档路径>

## 必须遵守

- 不可突破模块边界
- 不可绕过既有契约
- 不可擅自修改安全红线
- 不清楚逻辑时先沟通
- 改代码后必须更新文档和清理残留

## 输出物

- 代码
- 中文注释
- 文档更新
- 残留清理

## 待确认问题

- 暂无

## 实施记录

- 任务卡已创建
- 2026-05-09：已按只读审计方式检查 `citizenchain/runtime/governance/runtime-upgrade`、runtime 接线、桌面节点提交链路、SFID 人口快照接口、wumin 冷钱包识别规则、wuminapp runtime 升级编码与共享 fixture。

## 审计结论

- runtime-upgrade pallet 本体完成度较高：提案创建、WASM 对象层存储、联合投票回调、执行成功/失败状态映射、开发者直升开关、MODULE_TAG 归属判断均已落地。
- 当前不能判定为整体完成：桌面节点 `citizenchain/node` 的 `propose_runtime_upgrade` 签名请求和提交重建 call data 仍缺少 ADR-008 step3 的 `province` 与 `signer_admin_pubkey` 字段；SFID `app/voters/count` 人口快照响应也未返回这两个字段。
- wuminapp 端编码已按 ADR-008 step3 落地，并有 fixture 测试；但依赖 SFID 返回 `province` / `signer_admin_pubkey`，当前 SFID 响应缺字段会导致 wuminapp 页面直接报错。
- wumin 冷钱包已按文档采用 runtime 升级 32 字节哈希直签例外，普通 SCALE decoder 不再尝试解析完整 WASM call data。
- `runtime-upgrade/src/weights.rs` 仍明确标注为旧代码 benchmark 过估权重，storage proof 注释不反映当前 ProposalData/ProposalObject 路径，属于发布前残留。

## 主要风险

- P0：桌面节点提交 `propose_runtime_upgrade` 会构造旧版 call data，runtime 当前 extrinsic 需要 `province` 与 `signer_admin_pubkey`，因此桌面端升级提案提交链路不可用。
- P0：SFID 人口快照接口未下发 `province` 与 `signer_admin_pubkey`，会同时阻断桌面节点补齐 step3 字段和 wuminapp 在线端升级提案流程。
- P1：权重文件仍是旧实现过估值，虽然偏保守，但正式发布前应重新跑 benchmark，避免长期保留不准确 proof 注释。
- P2：runtime-upgrade 技术文档写的是目标完成态，但没有把上述跨端缺口标成未完成，容易误判模块已全链路完成。

## 验证记录

- 通过：`cargo test -p runtime-upgrade --lib`，16 passed。
- 通过：`cargo check -p runtime-upgrade --features runtime-benchmarks`。
- 通过：`flutter test test/proposal/runtime_upgrade/runtime_upgrade_service_test.dart`，3 passed。
- 通过：`flutter test test/signer/payload_decoder_test.dart`，33 passed。
- 未通过完整执行：`cargo check -p citizenchain --features runtime-benchmarks` 被 `citizenchain/runtime/build.rs` 阻止，原因是本地未设置 `WASM_FILE`，仓库策略要求使用 CI 统一 WASM；该结果不代表 runtime-upgrade 代码编译失败。

## 建议后续任务

- 修复 SFID `app/voters/count` 人口快照凭证：签名 payload 与响应补齐 `province`、`signer_admin_pubkey`。
- 修复 `citizenchain/node` runtime upgrade 提交流程：请求结果、前端类型、Tauri command、签名 payload 和 submit 重建 call data 全部对齐 ADR-008 step3。
- 重新生成 `runtime-upgrade` 权重文件并更新技术文档完成度说明。

## 完成信息

- 完成时间：2026-05-09 13:14:34
- 完成摘要：完成 runtime-upgrade 完成度只读审计：pallet 本体测试通过，但桌面节点/SFID ADR-008 step3 字段未对齐，整体不可判定完成；已记录验证结果和后续修复项。
- 对照清单：memory/07-ai/pre-submit-checklist.md
- 对照总标准：memory/07-ai/definition-of-done.md
