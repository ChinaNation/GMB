# 任务卡：节点UI改造-移除启停按钮和密码框-改为App生命周期托管

- 任务编号：20260425-083200
- 状态：done
- 所属模块：citizenchain/node
- 当前负责人：Claude
- 创建时间：2026-04-25 08:32:00

## 任务需求

Linux/Windows/macOS 三平台统一: 打开软件=启动节点, 退出软件=停止节点, 关窗=最小化. 删除启动节点/停止节点按钮, 删除启停密码框, 删除 start_node/stop_node Tauri command. set_grandpa_key/set_bootnode_key 保留自动重启, 同步修复 RocksDB LOCK bug (NodeHandle 加 shutdown 信号 + join 线程, 真正释放 Backend).

## 必读上下文

- memory/00-vision/project-goal.md
- memory/00-vision/trust-boundary.md
- memory/01-architecture/repo-map.md
- memory/03-security/security-rules.md
- memory/07-ai/agent-rules.md
- memory/07-ai/context-loading-order.md
- memory/01-architecture/citizenchain-target-structure.md
- memory/01-architecture/citizenchain/CITIZENCHAIN_TECHNICAL.md

## 模块模板

- 模板来源：memory/08-tasks/templates/citizenchain-node.md

### 默认改动范围

- `citizenchain/node`
- `memory/05-modules/citizenchain/node`

### 先沟通条件

- 修改节点 UI 与 node 的交互边界
- 修改桌面打包、sidecar 或安装包行为

## 模块执行清单

- 清单来源：memory/07-ai/module-checklists/citizenchain.md

# CitizenChain 模块执行清单

- 开工前先确认任务属于 `runtime`、`node`、`node` 或 `primitives`
- 关键 Rust 或前端逻辑必须补中文注释
- 改动链规则、存储或发布行为前必须先沟通
- 如果改动 `runtime` 且会影响 `wuminapp` 在线端或 `wumin` 冷钱包二维码签名/验签兼容性，必须先暂停单边修改，转为跨模块任务
- 触发项至少检查：`spec_version` / `transaction_version`、pallet index、call index、metadata 编码依赖、冷钱包 `pallet_registry` 与 `payload_decoder`
- 未把 `wuminapp` 在线端和 `wumin` 冷钱包的对应更新纳入本次执行范围前，不允许继续 runtime 改动
- 文档与残留必须一起收口

## 模块级完成标准

- 标准来源：memory/07-ai/module-definition-of-done/citizenchain.md

# CitizenChain 完成标准

- 改动范围和所属模块清晰
- 关键逻辑已补中文注释
- 文档已同步更新
- 影响链规则、存储或发布行为的点都已先沟通
- 残留已清理


## 必须遵守

- 不可突破模块边界
- 不可绕过既有契约
- 不可擅自修改安全红线
- 不清楚逻辑时先沟通
- 改代码后必须更新文档和清理残留

## 输出物

- 代码
- 中文注释
- 文档更新
- 残留清理

## 待确认问题

- 暂无

## 实施记录

- 任务卡已创建
- 2026-04-25 实施完成：
  - 后端 `citizenchain/node/src/ui/node_runner.rs`：`NodeHandle` 结构改为 `{ shutdown_tx: oneshot::Sender, thread: JoinHandle }`，新增 `Drop` 实现：先发 shutdown 信号 → `tokio::select!` 让 `task_manager.future()` 与 shutdown 任一退出 → 显式 `drop(task_manager)` 释放 Backend → `tokio_runtime.shutdown_timeout(10s)` → `JoinHandle::join()`，确保 RocksDB LOCK 真正释放
  - 后端 `ui/home/process/mod.rs`：删除 `start_node` / `stop_node` Tauri command 与 `verify_start_unlock_password`；`start_node_blocking` / `stop_node_blocking` 去掉 `unlock_password` 形参；`cleanup_on_exit` / 启停内 `state.node_handle.take()` 改为先取出再释放 state 锁后 drop，避免 join 期间阻塞 `get_node_status`
  - 后端 `ui/mod.rs`：删除 `home::process::start_node` / `stop_node` 注册；`.on_window_event` 拦截 `WindowEvent::CloseRequested` → `api.prevent_close()` + `window.hide()`（三平台统一）；setup 内 spawn 后台线程自动 `start_node_blocking`，避免阻塞窗口出现
  - 后端 `ui/settings/grandpa-address/mod.rs` / `bootnodes-address/mod.rs`：`set_*_key` 内"保存即重启"调用 `start_node_blocking(app.clone())` 去掉 unlock 透传；密码本身仍在 `set_*_key` 入口校验
  - 前端 `frontend/home/home-node/HomeNodeSection.tsx`：删除两按钮 / 两 modal / `starting` / `stopping` / 两 dialog state / 两 password state / `onStart` / `onStop` / `onNodeActionBusyChange` prop；保留状态展示与 3s 轮询
  - 前端 `frontend/api.ts`：删除 `startNode` / `stopNode`
  - 前端 `frontend/App.tsx`：删除 `nodeActionBusy` state 与 `disabled` prop 透传
  - 前端 `frontend/settings/settings-panel/SettingsSection.tsx`、`fee-address/WalletSection.tsx`、`node-key/NodeKeySection.tsx`：清理 `disabled` prop 链路（启停按钮已移除，该 prop 失去触发源，按 `feedback_no_remnants` 一并清理）
  - 验证：`WASM_FILE=… cargo check -p node` 0 error；改动文件无新增 warning；`tsc --noEmit` 0 error；`vite build` 通过（80 模块，403 KB）；零残留 grep 验证 `api.startNode` / `api.stopNode` / `invoke('start_node'|'stop_node')` / `onNodeActionBusyChange` / `nodeActionBusy` / `verify_start_unlock_password` / `process::start_node` / `process::stop_node` 全部为空
- 2026-04-25 二轮复核补丁：
  - **关键漏洞修复**：`window.hide()` → `window.minimize()`。原因：Tauri `hide()` 在 Windows/Linux 会让任务栏图标也消失，用户除强杀进程外无法找回窗口；`minimize()` 三平台均保留任务栏/Dock 图标可点击恢复，符合"关窗=最小化"语义
  - 文案：`frontend/settings/fee-address/WalletSection.tsx:216` `'未生成（请先启动节点）'` → `'未生成（节点启动中…）'`，与节点自动启动语义一致
  - 文档：`memory/05-modules/citizenchain/node/home/HOME_TECHNICAL.md` 重写 `process/mod.rs` 段，补充三平台生命周期表 + LOCK bug 修复说明 + 已删除清单
  - 重新 `npm run build` 后 dist 主 bundle hash 更新，dist 内 grep "启动节点 / 停止节点 / startNode / stopNode" 零命中
- 2026-04-25 三轮反转：用户重新定义"关窗 = 退出软件 = 停节点"，三平台统一。删除 `on_window_event` 拦截块，让 Tauri 默认 X 关窗 → App 退出 → `RunEvent::Exit` → `cleanup_on_exit` 停节点。macOS 黄色横线为系统原生 minimize，不触发 CloseRequested、不影响节点。HOME_TECHNICAL.md 行为表同步更新。
- 出范围（已识别但未本次处理）：
  - `frontend/ui-preview-{a,b,c}.html` 设计稿仍含"启动节点"按钮（不在 vite 入口、不影响生产，仅设计参考）
  - `HOME_TECHNICAL.md` 中 `identity/mod.rs` 段提到的 `set_node_name` Tauri command 早已删除（grep 全 src 零命中），属预先存在的文档遗留
  - 这两项建议作为单独的"文档/设计稿同步"任务卡处理，避免本任务范围扩散

## 完成信息

- 完成时间：2026-04-25 08:42:37
- 完成摘要：节点UI改造-移除启停按钮和密码框-改为App生命周期托管 完成: NodeHandle 加 shutdown 信号修 RocksDB LOCK bug + 三平台 App 生命周期托管节点 + 关窗即最小化
- 对照清单：memory/07-ai/pre-submit-checklist.md
- 对照总标准：memory/07-ai/definition-of-done.md
