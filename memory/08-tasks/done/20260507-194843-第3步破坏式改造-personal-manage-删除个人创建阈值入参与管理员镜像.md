# 任务卡：第3步破坏式改造 personal-manage 删除个人创建阈值入参与管理员镜像

- 任务编号：20260507-194843
- 状态：done
- 所属模块：citizenchain/runtime
- 当前负责人：Codex
- 创建时间：2026-05-07 19:48:44

## 任务需求

personal-manage 个人多签创建删除 admin_count/threshold 入参和旧编码，管理员与普通阈值唯一读取 admins-change，联动 wuminapp/wumin 编解码，更新文档并清理残留。

## 必读上下文

- memory/00-vision/project-goal.md
- memory/00-vision/trust-boundary.md
- memory/01-architecture/repo-map.md
- memory/03-security/security-rules.md
- memory/07-ai/agent-rules.md
- memory/07-ai/context-loading-order.md
- memory/01-architecture/citizenchain-target-structure.md
- citizenchain/CITIZENCHAIN_TECHNICAL.md
- citizenchain/runtime/README.md

## 模块模板

- 模板来源：memory/08-tasks/templates/citizenchain-runtime.md

### 默认改动范围

- `citizenchain/runtime`
- `citizenchain/governance`
- `citizenchain/issuance`
- `citizenchain/otherpallet`
- `citizenchain/transaction`
- 必要时联动 `primitives`

### 先沟通条件

- 修改 runtime 存储结构
- 修改资格模型
- 修改提案、投票、发行核心规则


## 模块执行清单

- 清单来源：memory/07-ai/module-checklists/citizenchain.md

# CitizenChain 模块执行清单

- 开工前先确认任务属于 `runtime`、`node`（含桌面端）或 `primitives`
- 关键 Rust 或前端逻辑必须补中文注释
- 改动链规则、存储或发布行为前必须先沟通
- 如果改动 `runtime` 且会影响 `wuminapp` 在线端或 `wumin` 公民钱包二维码签名/验签兼容性，必须先暂停单边修改，转为跨模块任务
- 触发项至少检查：`spec_version` / `transaction_version`、pallet index、call index、metadata 编码依赖、冷钱包 `pallet_registry` 与 `payload_decoder`
- 未把 `wuminapp` 在线端和 `wumin` 公民钱包的对应更新纳入本次执行范围前，不允许继续 runtime 改动
- 文档与残留必须一起收口

## 模块级完成标准

- 标准来源：memory/07-ai/module-definition-of-done/citizenchain.md

# CitizenChain 完成标准

- 改动范围和所属模块清晰
- 关键逻辑已补中文注释
- 文档已同步更新
- 影响链规则、存储或发布行为的点都已先沟通
- 残留已清理


## 必须遵守

- 不可突破模块边界
- 不可绕过既有契约
- 不可擅自修改安全红线
- 不清楚逻辑时先沟通
- 改代码后必须更新文档和清理残留

## 输出物

- 代码
- 中文注释
- 文档更新
- 残留清理

## 待确认问题

- 暂无

## 实施记录

- 任务卡已创建。
- 已破坏式调整 `personal-manage::propose_create`：删除 `admin_count / threshold` 入参，call 编码改为 `account_name + duoqian_admins + amount`。
- 已删除 `PersonalDuoqians` 中管理员列表、管理员数量、阈值镜像字段；管理员真源统一改为 `admins-change::Subjects`。
- 已删除 `CreateDuoqianAction` 中管理员数量与阈值字段。
- 已将创建提案阈值固定为拟定管理员全员数量，普通阈值统一由 `admins-change::derived_threshold` 派生。
- 已将创建通过执行顺序收口为：入金成功 → 激活 admins-change 主体 → 激活个人账户状态。
- 已联动 `duoqian-transfer` 测试查询、wuminapp 创建页/链上查询/ProposalData 解码、wumin 公民钱包 payload decoder。
- 已补充/更新中文注释，清理旧 personal create 阈值入参残留。
- 已更新 `PERSONAL_MANAGE_TECHNICAL.md`、`ADMINSCHANGE_TECHNICAL.md`、`ADR-015-account-admin-internal-vote.md`。
- 已确认本步骤未修改 `spec_version`。

## 验证记录

- `cargo fmt --manifest-path citizenchain/Cargo.toml --package personal-manage --package admins-change --package organization-manage --package duoqian-transfer`：通过。
- `cargo test --manifest-path citizenchain/Cargo.toml -p personal-manage --lib`：19 passed。
- `cargo test --manifest-path citizenchain/Cargo.toml -p admins-change --lib`：41 passed。
- `cargo test --manifest-path citizenchain/Cargo.toml -p internal-vote --lib`：86 passed。
- `cargo test --manifest-path citizenchain/Cargo.toml -p duoqian-transfer --lib`：20 passed。
- `flutter test test/duoqian/duoqian_manage_service_test.dart test/duoqian/duoqian_storage_codec_test.dart test/duoqian/duoqian_manage_storage_test.dart`：10 passed。
- `flutter test test/signer/payload_decoder_test.dart`：30 passed。
- `cargo test --manifest-path citizenchain/Cargo.toml -p organization-manage --lib`：24 passed。
- `flutter analyze` wuminapp 本次改动 8 个文件：No issues found。
- `flutter analyze` wumin 本次改动 3 个文件：No issues found。

## 完成信息

- 完成时间：2026-05-07 20:06:50
- 完成摘要：完成第3步 personal-manage 破坏式改造：删除个人创建 admin_count/threshold 入参与管理员镜像，管理员真源统一 admins-change，联动 wuminapp/wumin 编解码并完成回归测试。
- 对照清单：memory/07-ai/pre-submit-checklist.md
- 对照总标准：memory/07-ai/definition-of-done.md
