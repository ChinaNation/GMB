# 任务卡：治理 runtime 核查问题修复

- 任务编号：20260502-195022
- 状态：done
- 所属模块：citizenchain/runtime
- 当前负责人：Codex
- 创建时间：2026-05-02 19:50:22

## 任务需求

修复治理 runtime 核查确认的问题：InternalAdminProvider 一致性防线、admins-change storage version 与 integrity_test、ProposalOwner 归属与 data 编码、voting-engine 低风险 panic 和废弃清理 trait、runtime-upgrade 手工 finalize 残留、联合投票历史快照文档、benchmark 跨 pallet 状态 helper 与相关残留清理。

## 必读上下文

- memory/00-vision/project-goal.md
- memory/00-vision/trust-boundary.md
- memory/01-architecture/repo-map.md
- memory/03-security/security-rules.md
- memory/07-ai/agent-rules.md
- memory/07-ai/context-loading-order.md
- memory/01-architecture/citizenchain-target-structure.md
- citizenchain/CITIZENCHAIN_TECHNICAL.md
- citizenchain/runtime/README.md

## 模块模板

- 模板来源：memory/08-tasks/templates/citizenchain-runtime.md

### 默认改动范围

- `citizenchain/runtime`
- `citizenchain/governance`
- `citizenchain/issuance`
- `citizenchain/otherpallet`
- `citizenchain/transaction`
- 必要时联动 `primitives`

### 先沟通条件

- 修改 runtime 存储结构
- 修改资格模型
- 修改提案、投票、发行核心规则


## 模块执行清单

- 清单来源：memory/07-ai/module-checklists/citizenchain.md

# CitizenChain 模块执行清单

- 开工前先确认任务属于 `runtime`、`node`（含桌面端）或 `primitives`
- 关键 Rust 或前端逻辑必须补中文注释
- 改动链规则、存储或发布行为前必须先沟通
- 如果改动 `runtime` 且会影响 `wuminapp` 在线端或 `wumin` 冷钱包二维码签名/验签兼容性，必须先暂停单边修改，转为跨模块任务
- 触发项至少检查：`spec_version` / `transaction_version`、pallet index、call index、metadata 编码依赖、冷钱包 `pallet_registry` 与 `payload_decoder`
- 未把 `wuminapp` 在线端和 `wumin` 冷钱包的对应更新纳入本次执行范围前，不允许继续 runtime 改动
- 文档与残留必须一起收口

## 模块级完成标准

- 标准来源：memory/07-ai/module-definition-of-done/citizenchain.md

# CitizenChain 完成标准

- 改动范围和所属模块清晰
- 关键逻辑已补中文注释
- 文档已同步更新
- 影响链规则、存储或发布行为的点都已先沟通
- 残留已清理


## 必须遵守

- 不可突破模块边界
- 不可绕过既有契约
- 不可擅自修改安全红线
- 不清楚逻辑时先沟通
- 改代码后必须更新文档和清理残留

## 输出物

- 代码
- 中文注释
- 文档更新
- 残留清理

## 待确认问题

- 暂无

## 实施记录

- 任务卡已创建
- 已修复 `InternalAdminProvider` 强一致契约：trait 文档声明 `is_*` 与 `get_*_list` 必须一致，内部/联合提案创建写快照后校验发起人仍在快照内，drift 时 defensive + 回滚。
- 已将 `admins-change` 的 `STORAGE_VERSION` 从 2 调整为 1，并移除 `integrity_test` 的 `std` 守卫。
- 已把 `admins-change` 管理员替换提案 data 改为裸 `AdminReplacementAction`，由 `ProposalOwner = adm-rep-v1` 认领；`voting-engine` 新增 `is_proposal_owner` 查询。
- 已删除 `governance_org_pass_threshold` 兼容别名并替换调用方。
- 已删除 `JointVoteEngine::cleanup_joint_proposal` / `InternalVoteEngine::cleanup_internal_proposal` 空实现占位。
- 已删除 `runtime-upgrade::finalize_joint_vote` 手工 Root extrinsic、对应权重项、benchmark 项和 runtime 免费分支，保留 call index 1 空缺。
- 已将 `ExecutionRetryGraceBlocks` 改为 `BlockNumber` 类型的 `VotingExecutionRetryGraceBlocks` 参数，避免 `ConstU32` 类型耦合。
- 已为 `admins-change` benchmark 改用 `voting-engine` 的 runtime-benchmarks helper，并补充 feature 透传。
- 已更新 `admins-change`、`voting-engine`、`runtime-upgrade`、`resolution-issuance`、`onchain-transaction` 技术文档。

## 验证记录

- `cargo test -p voting-engine --lib`：通过，74 passed。
- `cargo test -p admins-change --lib`：通过，28 passed。
- `cargo test -p runtime-upgrade --lib`：通过，16 passed。
- `cargo check -p voting-engine --features runtime-benchmarks`：通过。
- `cargo check -p admins-change --features runtime-benchmarks`：通过。
- `cargo check -p runtime-upgrade --features runtime-benchmarks`：通过。
- `cargo check -p resolution-issuance --features runtime-benchmarks`：通过。
- `cargo check -p duoqian-manage`：通过。
- `cargo check -p duoqian-transfer`：通过。
- `cargo check -p resolution-destro`：通过。
- `cargo check -p citizenchain`：未通过执行环境门禁，build.rs 要求设置 `WASM_FILE`，未进入 runtime 代码编译错误。
- `git diff --check`：通过。
- 残留扫描：目标范围内无 `governance_org_pass_threshold`、`兼容旧调用名`、空清理 trait、`runtime-upgrade::finalize_joint_vote` 入口、`finalize_create` 字符串残留。

## 完成信息

- 完成时间：2026-05-02 20:10:49
- 完成摘要：完成治理 runtime 核查问题修复：快照一致性防线、admins-change 版本与 integrity、ProposalOwner 认领、runtime-upgrade 手工入口删除、benchmark helper、文档与残留清理，并通过相关 pallet 测试和 benchmark feature 检查。
- 对照清单：memory/07-ai/pre-submit-checklist.md
- 对照总标准：memory/07-ai/definition-of-done.md
