# 治理 runtime 审计 4 项后续修复

## 背景

`6776dee1 修复` 已修复 voting-engine + admins-change 两轮深度审计的高/中危项。本任务承接复审报告剩余 4 项：

1. 删除 voting-engine `retry_passed_proposal_for` / `cancel_passed_proposal_for` 兼容层（违反 no-compatibility 铁律）
2. 跑 benchmark 替换占位 weight（voting-engine + admins-change 等 8 个 pallet）
3. admins-change `genesis_build` 8 处 `expect` 重构（抽 helper + panic 信息携带 `shenfen_id`）
4. admins-change 补 3 条边界测试（全员循环、互斥锁并发、跨省隔离）

## 边界

- **spec_version 不动**：用户拍板。当前 spec_version=0（创世链未升过 runtime），删除业务 extrinsic 的 call_index 不需要 bump
- **严守 no-compatibility 死规则**：业务 pallet 的 `execute_xxx` extrinsic 全部删除，前端直接调 `votingEngine.retryPassedProposal` / `cancelPassedProposal`
- **chainspec.json 不改**：按 chainspec 冻结铁律
- **本会话先做链端**；前端 wuminapp / wumin / sfid-frontend 联动需用户确认范围后另起任务卡或本会话续做
- **benchmark 留给用户手动跑**（需要 gh auth + CI WASM）

## 改动文件清单

### 链端（本会话）

| 文件 | 改动 |
|---|---|
| `citizenchain/runtime/governance/voting-engine/src/lib.rs:2252-2260` | 删 `retry_passed_proposal_for` / `cancel_passed_proposal_for` 两个 pub fn |
| `citizenchain/runtime/governance/admins-change/src/lib.rs:255-333` | genesis_build 抽 helper `build_builtin_institution`，消除重复 |
| `citizenchain/runtime/governance/admins-change/src/lib.rs:486-498` | 删 `execute_admin_replacement` (call_index=1) |
| `citizenchain/runtime/governance/admins-change/src/weights.rs` | 删 `execute_admin_replacement` weight fn + trait |
| `citizenchain/runtime/governance/admins-change/src/lib.rs` 测试段 | tests 内 execute_admin_replacement 改用 VotingEngine::retry_passed_proposal |
| `citizenchain/runtime/governance/admins-change/src/lib.rs` 测试末尾 | 新增 3 条边界测试 |
| `citizenchain/runtime/governance/grandpakey-change/src/lib.rs:343-362` | 删 `execute_replace_grandpa_key` + `cancel_failed_replace_grandpa_key` |
| `citizenchain/runtime/governance/grandpakey-change/src/weights.rs` | 删两个 weight fn |
| `citizenchain/runtime/governance/grandpakey-change/src/lib.rs` 测试段 | 受影响测试改写 |
| `citizenchain/runtime/governance/resolution-destro/src/lib.rs:202` | 删 `execute_destroy` |
| `citizenchain/runtime/governance/resolution-destro/src/lib.rs` 测试段 | 改写 |
| `citizenchain/runtime/transaction/duoqian-transfer/src/lib.rs:574-598` | 删 `execute_transfer` + `execute_safety_fund_transfer` + `execute_sweep_to_main` |
| `citizenchain/runtime/transaction/duoqian-transfer/src/lib.rs` 测试段 | 改写 |

### 前端（已完成 2026-05-02）

扫描结果：
- **wuminapp（Flutter 热钱包）**: 0 处命中——本就没有 retry/cancel UI
- **sfid / cpms 前后端**: 0 处命中
- **citizenchain/node**: 1 处注释更新（`signing.rs:537-541` 把 Phase 4 删除说明写进 doc）
- **wumin（Tauri 冷钱包 decoder）**: 5 个文件改动
  - `lib/signer/pallet_registry.dart`: 删 6 个旧 wrapper call 常量 + 新增 `retryPassedProposalCall=4` / `cancelPassedProposalCall=5`
  - `lib/signer/payload_decoder.dart`: 删 7 个旧业务分支 + 新增 VotingEngine 下 retry/cancel 解码（cancel 含 `BoundedVec<u8>` reason）
  - `lib/signer/action_labels.dart`: 删 7 个旧 action label + 新增 retry/cancel 中文标签
  - `test/signer/payload_decoder_test.dart`: 删 7 个旧测试 + 补 retry / cancel(空 reason / utf8 reason) + 旧 wrapper 拒绝识别回归测试
  - `test/signer/pallet_registry_test.dart`: 测试断言同步

### Benchmark（待用户手动）

执行 `./citizenchain/scripts/benchmark.sh` 跑 voting_engine / admins_change / duoqian_manage / duoqian_transfer / runtime_upgrade / resolution_issuance / resolution_destro / grandpakey_change

## 验收

1. ✓ `cargo check --features runtime-benchmarks -p admins-change -p voting-engine -p grandpakey-change -p resolution-destro -p duoqian-transfer -p runtime-upgrade -p resolution-issuance -p duoqian-manage --release` 0 error 0 warning
2. ✓ `cargo test --release` 8 pallet 共 214 条测试 0 失败
3. ✓ `grep -rn "_for(" citizenchain/runtime/governance/voting-engine/src/` 返回 0
4. ✓ `grep -rn "execute_admin_replacement\|execute_replace_grandpa_key\|execute_destroy\|execute_transfer\|execute_safety_fund_transfer\|execute_sweep_to_main\|cancel_failed_replace_grandpa_key" citizenchain/runtime/` 仅历史 comment
5. ✓ 新增 3 条 admins-change 测试全部通过
6. ✓ wumin `flutter analyze` 0 issues + `flutter test` 105 测试全过
