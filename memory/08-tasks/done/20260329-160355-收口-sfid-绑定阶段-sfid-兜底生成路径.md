# 任务卡：收口 sfid 绑定阶段绕过 sfid 工具的兜底生成路径，要求绑定前必须先使用 sfid 工具生成 sfid，保持全系统只有一套 sfid 生成口径，并同步更新前后端提示与文档

- 任务编号：20260329-160355
- 状态：done
- 所属模块：sfid/backend
- 当前负责人：Codex
- 创建时间：2026-03-29 16:03:55
- 完成时间：2026-03-29 16:32:00

## 任务需求

收口 sfid 绑定阶段绕过 sfid 工具的兜底生成路径，要求绑定前必须先使用 sfid 工具生成 sfid，保持全系统只有一套 sfid 生成口径，并同步更新前后端提示与文档

## 必读上下文

- memory/00-vision/project-goal.md
- memory/00-vision/trust-boundary.md
- memory/01-architecture/repo-map.md
- memory/03-security/security-rules.md
- memory/07-ai/agent-rules.md
- memory/07-ai/context-loading-order.md
- sfid/README.md
- sfid/SFID_TECHNICAL.md

## 模块模板

- 模板来源：memory/08-tasks/templates/sfid-backend.md

### 默认改动范围

- `sfid/backend`
- 必要时联动 `sfid/deploy`

### 先沟通条件

- 修改 permit 模型
- 修改账户绑定规则
- 修改数据库结构


## 模块执行清单

- 清单来源：memory/07-ai/module-checklists/sfid.md

# SFID 模块执行清单

- 不保存原始实名
- permit、绑定、数据库结构变化前必须先沟通
- 关键接口和数据模型必须补中文注释
- 文档与残留必须一起收口


## 模块级完成标准

- 标准来源：memory/07-ai/module-definition-of-done/sfid.md

# SFID 完成标准

- 仍然满足 SFID 不保存原始实名
- 关键接口、数据模型与边界判断已补中文注释
- 文档已同步更新
- permit、绑定、数据库结构变化已先沟通
- 残留已清理


## 必须遵守

- 不可突破模块边界
- 不可绕过既有契约
- 不可擅自修改安全红线
- 不清楚逻辑时先沟通
- 改代码后必须更新文档和清理残留

## 输出物

- 代码
- 中文注释
- 文档更新
- 残留清理

## 待确认问题

- 暂无

## 实施记录

- 后端 `admin_bind_confirm` 已移除绑定阶段 `deterministic_sfid_code` 兜底路径，未先生成 `sfid` 时返回 `3011 / sfid must be generated before binding`。
- 前端绑定流程已增加本地门禁与错误提示，管理员必须先使用 `SFID` 码生成工具后才能进入绑定确认。
- 已补 2 条回归测试，覆盖“未预生成拒绝绑定”与“消费预生成 `sfid` 成功绑定”。
- 启动演示数据 `seed_demo_record` 也已改为复用正式 `SFID` 生成工具，避免空库启动时再写入旧格式 `SFID-...` 记录。
- `SFID` 技术文档与统一架构文档已同步更新为“绑定前必须先生成 `sfid`，绑定阶段不再兜底生成”。

## 验证记录

- `cargo test --manifest-path sfid/backend/Cargo.toml`
- `npm --prefix sfid/frontend run build`
- `git diff --check -- sfid memory`
