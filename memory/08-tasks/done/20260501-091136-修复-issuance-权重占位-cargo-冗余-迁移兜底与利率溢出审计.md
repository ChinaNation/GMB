# 任务卡：修复 issuance 权重占位 Cargo 冗余 迁移兜底与利率溢出审计

- 任务编号：20260501-091136
- 状态：done
- 所属模块：citizenchain/runtime/issuance
- 当前负责人：Codex
- 创建时间：2026-05-01 09:11:36

## 任务需求

修复 resolution-issuance 权重占位、fullnode-issuance sp-std 冗余、resolution-issuance migration 冗余兜底、shengbank-interest 利率乘法溢出审计缺口，并同步文档、注释和残留清理。

## 必读上下文

- memory/00-vision/project-goal.md
- memory/00-vision/trust-boundary.md
- memory/01-architecture/repo-map.md
- memory/03-security/security-rules.md
- memory/07-ai/agent-rules.md
- memory/07-ai/context-loading-order.md
- <补充该模块对应技术文档路径>

## 必须遵守

- 不可突破模块边界
- 不可绕过既有契约
- 不可擅自修改安全红线
- 不清楚逻辑时先沟通
- 改代码后必须更新文档和清理残留

## 输出物

- 代码
- 中文注释
- 文档更新
- 残留清理

## 待确认问题

- 暂无

## 实施记录

- 任务卡已创建
- 已检查目标文件和当前工作区 diff；本次只处理 issuance 下四个审查发现，不回退其他线程/前序任务留下的改动。
- 已删除 `fullnode-issuance` 未使用的 `sp-std` 依赖及 `sp-std/std` feature 传播。
- 已将 `resolution-issuance::migration` 收紧为 fresh genesis 口径下只推进 `StorageVersion`，删除 `AllowedRecipients::is_empty()` 兜底路径。
- 已为 `shengbank-interest` 新增 `ShengBankInterestOverflow` 事件，并把利息乘法从 `saturating_mul` 改为 `checked_mul`，溢出时发事件、写日志并让该年度结算失败。
- 已同步更新 `FULLNODE_TECHNICAL.md`、`SHENGBANK_TECHNICAL.md`、`RESOLUTIONISSUANCE_TECHNICAL.md` 的依赖、审计事件、迁移和 benchmark 说明。
- 已清理 `offchain-transaction/src/benchmarks.rs` 的空 `#[benchmarks]` 宏残留；该空宏会在全 runtime `runtime-benchmarks` 构建时生成非法代码，阻断本任务的 benchmark node 构建。
- 已尝试 `cargo build --release --features runtime-benchmarks`：失败原因是 runtime build 要求 `WASM_FILE`。
- 已尝试 `WASM_BUILD_FROM_SOURCE=1 cargo build --release --features runtime-benchmarks`：失败原因是 wasm 子构建在 `wasm32v1-none` 下拉入带 `std` 的 `serde_core` / `byte-slice-cast`。
- 已联网下载 CI `citizenchain-wasm`，并用 `WASM_FILE=.../citizenchain.compact.compressed.wasm cargo build --release --features runtime-benchmarks` 成功构建 benchmark CLI。
- 已尝试运行 `resolution_issuance` benchmark：失败原因是当前 CI WASM 不包含 Benchmark Runtime API。
- 已将 `resolution-issuance/src/weights.rs` 从含糊占位改为明确的偏高保守 fallback，并记录发布前仍需准备 benchmark runtime WASM 后重新生成正式权重。
- 已同步 `offchain-transaction/STEP2A_RUNTIME.md`，说明不再保留空 `#[benchmarks]` 模块以及正式 benchmark 的恢复条件。
- 已创建后续任务卡 `memory/08-tasks/open/20260501-092408-准备-benchmark-runtime-wasm-并重新生成-resolution-权重.md`，专门跟进 benchmark runtime WASM 与正式权重重生。
- 已执行 `cargo fmt -p resolution-issuance -p fullnode-issuance -p shengbank-interest -p offchain-transaction`。
- 已执行并通过 `cargo test -p resolution-issuance`（16 passed）。
- 已执行并通过 `cargo test -p fullnode-issuance`（19 passed）。
- 已执行并通过 `cargo test -p shengbank-interest`（19 passed）。
- 已执行并通过 `cargo check -p resolution-issuance --features runtime-benchmarks`。
- 已执行并通过 `cargo check -p fullnode-issuance --features runtime-benchmarks`。
- 已执行并通过 `cargo check -p shengbank-interest --features runtime-benchmarks`。
- 已执行并通过 `cargo check -p offchain-transaction --features runtime-benchmarks`。
- 已执行并通过 `git diff --check`。

## 完成信息

- 完成时间：2026-05-01 09:24:21
- 完成摘要：完成 issuance 审查发现修复：清理 fullnode sp-std，收紧 resolution migration，补 shengbank 利率溢出审计事件，清理 offchain 空 benchmark 宏阻塞，更新文档与验证；正式 resolution benchmark 权重生成因缺 benchmark runtime WASM 拆为后续任务。
- 对照清单：memory/07-ai/pre-submit-checklist.md
- 对照总标准：memory/07-ai/definition-of-done.md
