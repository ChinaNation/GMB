# CPMS 两级管理员权限模型

## 任务需求

- CPMS 只保留 `ADMIN` 和 `OPERATOR` 两种管理员。
- 超级管理员拥有所有权限。
- 系统设置、创建/停用/删除操作员仅超级管理员可操作。
- 修改公民状态、档案创建/编辑/查询、投票账户绑定、档案码生成/打印两种管理员都可操作。

## 完成情况

- 后端新增角色集合守卫和档案业务守卫。
- 档案业务接口统一允许 `ADMIN + OPERATOR`。
- 公民状态修改接口改为两种管理员可操作。
- 前端系统设置、管理员管理路由单独限制为超级管理员。
- CPMS 文档补充权限矩阵。
