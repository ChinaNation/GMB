任务需求：
修复统一签名协议复查发现的遗漏、残留和命名不一致问题；覆盖文档、注释和代码，不保留旧协议、旧目录和旧兼容分支。

所属模块：
QR 协议文档、SFID 管理员目录文档、wumin 钱包密钥读取、wuminapp 签名与链下支付说明。

完成项：
- QR action registry 删除 `activate_admin / GMB_ACTIVATE` 当前态描述，统一为 `activate_admin_account / GMB_ACTIVATE_SUBJECT_V1`。
- SFID 目录命名文档从 `sheng_admins / shi_admins` 更新为统一 `admins`。
- runtime `sfid-system/src/sheng_admins/` 文档说明改为链上 pallet 内部命名，不再要求与 SFID 后端/前端同名。
- 删除 wumin 助记词读取中的明文格式兼容迁移分支，删除 `MnemonicCipher.isEncrypted` 残留测试。
- 管理员激活二维码的人读字段 `pubkey` 改为 SS58 地址，机读 `sign_request.body.pubkey` 继续保持 0x hex。
- 修正 wuminapp `QrSigner`、wumin `QrSigner`、SFID 公钥工具等残留注释，统一描述为当前 `sign_request` 机读字段和人读字段规则。
- 清理链下支付文档和代码注释中“后续冷钱包 QR 扫签”口径，统一为“当前仅热钱包；冷钱包必须另行设计可独立验证的 PaymentIntent 原文协议后才能接入”。
- 删除不存在的 `STEP2C_II_RECEIVE_QR.md` 链接，并删除已经不属于当前目标状态的链下支付冷钱包文档。
- 清理测试中旧协议命名残留，保留当前协议对缺字段、多余尾字段、无效 payload 的拒签覆盖。

验证记录：
- `rg` 残留扫描通过：未命中 `GMB_ACTIVATE`、`STEP2C_II_RECEIVE_QR`、`Step 2c-iii`、`兼容旧版明文`、`isEncrypted(`、`activated_admins_v1/v2`、`feedback_pubkey_format_rule`、前后端 `sheng_admins/shi_admins` 业务目录写法。
- `dart format`：wumin、wuminapp 相关 Dart 文件已格式化。
- `cargo fmt --manifest-path citizenchain/node/Cargo.toml` 通过。
- `cargo fmt --manifest-path sfid/backend/Cargo.toml` 通过。
- `flutter test test/signer/payload_decoder_test.dart test/signer/offline_sign_service_test.dart test/signer/pallet_registry_test.dart test/wallet/mnemonic_cipher_test.dart` 通过。
- `flutter test test/signer/qr_signer_test.dart test/qr/qr_sign_session_test.dart test/governance/admins-change/institution_admin_service_test.dart` 通过。
- `flutter analyze`：wumin 通过。
- `flutter analyze`：wuminapp 通过。
- `cargo check --manifest-path citizenchain/node/Cargo.toml` 通过。
- `cargo check --manifest-path sfid/backend/Cargo.toml` 通过。
- `git diff --check` 通过。
