# 任务卡：新增首页交易矿工热钱包

- 任务编号：20260501-094149
- 状态：done
- 所属模块：citizenchain/node
- 当前负责人：Codex
- 创建时间：2026-05-01 09:41:49

## 任务需求

在节点软件首页交易钱包管理中置顶显示本机矿工热钱包，不显示删除按钮；普通钱包继续走冷钱包二维码签名，矿工热钱包使用本机 powr keystore 经设备密码确认后直接签名并提交交易；更新文档并完成验证。

## 必读上下文

- memory/00-vision/project-goal.md
- memory/00-vision/trust-boundary.md
- memory/01-architecture/repo-map.md
- memory/03-security/security-rules.md
- memory/07-ai/agent-rules.md
- memory/07-ai/context-loading-order.md
- memory/01-architecture/citizenchain-target-structure.md
- citizenchain/CITIZENCHAIN_TECHNICAL.md

## 模块模板

- 模板来源：memory/08-tasks/templates/citizenchain-node.md

### 默认改动范围

- `citizenchain/node`
- `memory/05-modules/citizenchain/node`

### 先沟通条件

- 修改桌面前端与节点后端的交互边界
- 修改桌面打包、sidecar 或安装包行为

## 模块执行清单

- 清单来源：memory/07-ai/module-checklists/citizenchain.md

# CitizenChain 模块执行清单

- 开工前先确认任务属于 `runtime`、`node`（含桌面端）或 `primitives`
- 关键 Rust 或前端逻辑必须补中文注释
- 改动链规则、存储或发布行为前必须先沟通
- 如果改动 `runtime` 且会影响 `wuminapp` 在线端或 `wumin` 冷钱包二维码签名/验签兼容性，必须先暂停单边修改，转为跨模块任务
- 触发项至少检查：`spec_version` / `transaction_version`、pallet index、call index、metadata 编码依赖、冷钱包 `pallet_registry` 与 `payload_decoder`
- 未把 `wuminapp` 在线端和 `wumin` 冷钱包的对应更新纳入本次执行范围前，不允许继续 runtime 改动
- 文档与残留必须一起收口

## 模块级完成标准

- 标准来源：memory/07-ai/module-definition-of-done/citizenchain.md

# CitizenChain 完成标准

- 改动范围和所属模块清晰
- 关键逻辑已补中文注释
- 文档已同步更新
- 影响链规则、存储或发布行为的点都已先沟通
- 残留已清理


## 必须遵守

- 不可突破模块边界
- 不可绕过既有契约
- 不可擅自修改安全红线
- 不清楚逻辑时先沟通
- 改代码后必须更新文档和清理残留

## 输出物

- 代码
- 中文注释
- 文档更新
- 残留清理

## 待确认问题

- 暂无

## 实施记录

- 任务卡已创建
- 已在后端 `home/transaction` 动态注入 `矿工热钱包`，置顶展示且不写入 `cold-wallets.json`，删除接口会拒绝该钱包。
- 已扩展钱包类型字段：`minerHot` 使用本地矿工签名路径，`cold` 继续使用离线 QR 签名路径。
- 已新增 `submit_miner_transfer` Tauri 命令：先校验设备开机密码，再签发进程内一次性令牌并调用节点 RPC。
- 已新增 `transaction_submitMinerTransfer` 节点 RPC：使用 `powr` keystore 签名 `Balances::transfer_keep_alive`，返回交易哈希；直接本机 RPC 调用必须携带一次性令牌。
- 已更新前端钱包管理列表：矿工热钱包仅显示名称、地址、余额，不显示删除按钮；不再出现“本机热钱包”。
- 已更新前端转账签名弹窗：矿工热钱包输入设备开机密码后直接提交，普通钱包仍显示二维码签名流程。
- 已同步更新 `HOME_TECHNICAL.md`、`NODE_TECHNICAL.md`、`KEYSTORE_TECHNICAL.md`。

## 验证记录

- `npm run build`（`citizenchain/node/frontend`）：通过。
- `cargo fmt --manifest-path citizenchain/node/Cargo.toml`：通过。
- `cargo check -p node`：首次未设置 `WASM_FILE` 被 runtime 构建策略阻断。
- `WASM_FILE=/private/tmp/dummy_wasm.wasm cargo check -p node`（`citizenchain`）：通过；剩余输出为仓库既有 unsafe/dead_code warnings。
- 残留扫描：`本机热钱包` 在交易钱包链路无命中；`矿工账户` 在交易钱包链路无命中。
