# 任务卡：cid 本地启动端口与数据库残留修复

- 任务编号：20260530-193458
- 状态：done
- 所属模块：cid
- 当前负责人：Codex
- 创建时间：2026-05-30 19:34:58

## 任务需求

统一本地开发前端为 localhost:5179，修复 cid-run.sh 端口占用处理，并清理 admins 表旧状态/签名字段残留导致的持久化报错。

## 必读上下文

- memory/00-vision/project-goal.md
- memory/00-vision/trust-boundary.md
- memory/01-architecture/repo-map.md
- memory/03-security/security-rules.md
- memory/07-ai/agent-rules.md
- memory/07-ai/context-loading-order.md
- citizencode/README.md
- citizencode/CID_TECHNICAL.md

## 模块模板

- 模板来源：memory/08-tasks/templates/citizencode-backend.md

### 默认改动范围

- `citizencode/backend`
- 必要时联动 `citizencode/deploy`

### 先沟通条件

- 修改 permit 模型
- 修改账户绑定规则
- 修改数据库结构


## 模块执行清单

- 清单来源：memory/07-ai/module-checklists/citizencode.md

# CID 模块执行清单

- 不保存原始实名
- permit、绑定、数据库结构变化前必须先沟通
- 关键接口和数据模型必须补中文注释
- 文档与残留必须一起收口


## 模块级完成标准

- 标准来源：memory/07-ai/module-definition-of-done/citizencode.md

# CID 完成标准

- 仍然满足 CID 不保存原始实名
- 关键接口、数据模型与边界判断已补中文注释
- 文档已同步更新
- permit、绑定、数据库结构变化已先沟通
- 残留已清理


## 必须遵守

- 不可突破模块边界
- 不可绕过既有契约
- 不可擅自修改安全红线
- 不清楚逻辑时先沟通
- 改代码后必须更新文档和清理残留

## 输出物

- 代码
- 中文注释
- 文档更新
- 残留清理

## 待确认问题

- 暂无

## 实施记录

- 任务卡已创建
- 已确认前端 `dev` 脚本仍硬编码 `127.0.0.1:5179`,与 Passkey `localhost` RP 配置不一致。
- 已确认本地 `admins` 表残留 `status` 和云端签名字段,导致当前后端插入管理员快照失败。
- 已将前端开发入口统一为 `http://localhost:5179`,并开启 `strictPort` 防止端口漂移。
- 已增强 `cid-run.sh`:启动前停止本机 `com.gmb.citizencode-backend` launchd 服务、清理 `8899/5179`,执行本地最终结构清理,后端改为先 build 再直接启动二进制,并增加父进程监控避免 Ctrl-C 后残留监听。
- 已新增 `016_finalize_admin_no_status.sql`,清理 `admins.status`、云端管理员签名字段和旧 `v_super/v_operator` 视图,收口为 `v_sheng_admins / v_shi_admins`。
- 已执行本地 SQL 清理；当时 `admins` 表仍包含旧合并姓名列，该历史表结构现已废弃。
- 已更新 CID 架构文档、后端布局文档、前端布局文档和本地启动说明。
- 已完成验证:`npm run build`、`cargo fmt`、`cargo check`、`cargo test`、`bash -n cid-run.sh`、`git diff --check`、`./cid-run.sh` 实启停健康检查。

## 完成信息

- 完成时间：2026-05-30 19:46:03
- 完成摘要：本地开发入口统一为 localhost:5179, 修复 cid-run.sh 端口清理和 Ctrl-C 残留问题, 清理 admins 旧状态/签名字段并更新文档。
- 对照清单：memory/07-ai/pre-submit-checklist.md
- 对照总标准：memory/07-ai/definition-of-done.md
