任务需求：
全仓库统一 WUMIN_QR_V1 签名协议展示与验证规则，保持 payload_hex 真实签名内容不变；人机交互一律使用中文与 SS58 地址；wumin / wuminapp 只能在可独立验证时绿色通过，不能独立验证时红色拒签；同步更新文档、中文注释并清理旧残留。

所属模块：
QR 协议、wumin 公民钱包、wuminapp 热钱包、SFID、CPMS、citizenchain node

输入文档：
- memory/00-vision/project-goal.md
- memory/00-vision/trust-boundary.md
- memory/01-architecture/repo-map.md
- memory/01-architecture/qr/qr-protocol-spec.md
- memory/01-architecture/qr/qr-signing-recognition.md
- memory/01-architecture/qr/qr-action-registry.md
- memory/03-security/security-rules.md
- memory/07-ai/unified-protocols.md
- memory/05-modules/sfid/backend/BACKEND_LAYOUT.md
- memory/05-modules/sfid/backend/citizens/CITIZENS_TECHNICAL.md
- memory/05-modules/cpms/backend/dangan/DANGAN_TECHNICAL.md

必须遵守：
- 不新增第二套扫码协议，唯一协议仍为 WUMIN_QR_V1。
- 不改变真实签名内容，签名对象仍为 sign_request.body.payload_hex 的原始字节。
- 不允许黄色盲签、兼容旧格式、旧字段别名或不可验证放行。
- 所有人机可见账户字段必须显示 SS58 地址；机器验签字段可以保留 0x 哈希/公钥。
- 代码注释使用中文，文档同步更新，残留注释和旧规则清理。

输出物：
- wumin 公民钱包签名识别、展示与拒签规则代码
- wuminapp 电子护照绑定签名独立验证代码
- SFID / CPMS / citizenchain 签名请求展示字段收口
- QR 协议文档、action registry、模块文档更新
- 单元测试更新
- 残留注释和旧规则清理

验收标准：
- 全仓库只有 WUMIN_QR_V1 作为扫码签名协议。
- payload_hex 真实签名内容保持不变。
- wumin / wuminapp 独立验证通过才允许签名，否则红色拒签。
- 人机展示使用中文和 SS58 地址。
- Passkey 更新、管理员治理、CPMS 档案删除、SFID 电子护照绑定、链上交易签名展示统一。
- 相关测试通过，文档已更新，旧残留已清理。

完成摘要：
- 2026-05-31：wumin 公民钱包新增 `reviewFields` 展示层，机器验真字段与人机确认字段分离；不能独立解码或 display 不一致时红色拒签。
- 2026-05-31：SFID 管理员治理、Passkey 更新、CPMS 档案删除、citizenchain 治理签名展示字段统一收口，账户展示统一转 SS58。
- 2026-05-31：wuminapp 电子护照绑定签名新增 `citizen_bind` payload 独立解析与 display 逐项校验；链下支付公民钱包签名入口下线，避免对不可解码 hash 放行。
- 2026-05-31：更新 QR 协议、action registry、SFID/CPMS/wuminapp 模块文档，删除旧冷钱包链下支付步骤文档并清理旧注释。

验收记录：
- `flutter test test/signer/payload_decoder_test.dart test/signer/offline_sign_service_test.dart test/signer/pallet_registry_test.dart`（wumin，通过）
- `flutter test test/qr/qr_sign_session_test.dart test/signer/qr_signer_test.dart test/myid_page_test.dart`（wuminapp，通过）
- `flutter analyze`（wuminapp，通过）
- `cargo check --manifest-path sfid/backend/Cargo.toml`（通过）
- `cargo test --manifest-path sfid/backend/Cargo.toml`（通过，70 个测试）
- `cargo check --manifest-path cpms/backend/Cargo.toml`（通过）
- `cargo test --manifest-path cpms/backend/Cargo.toml`（通过，31 个测试）
- `cargo check --manifest-path citizenchain/node/Cargo.toml`（通过）
- `git diff --check`（通过）
