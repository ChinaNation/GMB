任务需求：
彻底删除 SFID 后端历史聚合快照主数据壳，改为结构化业务表唯一持久化真源。公权机构、公安局、私权机构、公民、管理员、CPMS、账户、资料库、审计和链路幂等都必须按各自模块表读写，不允许继续保留第二套主数据。

所属模块：
SFID

必须遵守：
- 不允许保留历史快照表或运行期分片缓存作为业务主数据入口。
- 不允许通过兼容旧格式、双写旧格式、从旧格式回填的方式解决。
- 机构、个人唯一身份只认 `sfid_number`，不得新增第二身份键。
- 后端源码继续直接以 `sfid/backend/` 为根目录，不恢复 `backend/src/`。
- 前后端公权机构命名继续统一使用 `gov`。
- 联邦/市管理员查询必须在 SQL 层带省市范围条件，不允许先全量加载后过滤。
- 不涉及投票流程。

预计修改目录：
- `sfid/backend/core/`：新增数据库连接、事务、分页和当前 schema 初始化能力，涉及代码。
- `sfid/backend/main.rs`：删除历史聚合初始化、快照加载保存和旧路由残留，改用结构化数据库状态，涉及代码和残留清理。
- `sfid/backend/admins/`：管理员、登录、Passkey、session、安全授权改为结构化表读写，涉及代码。
- `sfid/backend/citizens/`：公民绑定、年度导入、身份查询和扫码幂等改为结构化表读写，涉及代码。
- `sfid/backend/cpms/`：CPMS 安装授权和站点状态改为结构化表读写，涉及代码。
- `sfid/backend/gov/`：公安局和自动公权机构对账直接写 `ids / subjects / gov / accounts`，涉及代码。
- `sfid/backend/private/`：私权机构新增、查询和详情直接写结构化表，涉及代码。
- `sfid/backend/subjects/`：保留主体模型、公共校验、非法人能力和 SQL 查询，涉及代码。
- `sfid/backend/accounts/`：机构账户改为只读写 `accounts` 表，涉及代码。
- `sfid/backend/docs/`：机构资料库改为只读写 `docs` 表，涉及代码。
- `memory/01-architecture/sfid/`：更新 SFID 技术文档，删除历史快照方案说明，涉及文档。
- `memory/08-tasks/`：更新任务记录，涉及文档。

验收标准：
- `cargo check` 通过。
- `cargo check --tests` 通过。
- 后端源码和当前 SFID 架构文档不再命中历史聚合快照符号。
- 数据库 schema 初始化不再创建历史快照表，并主动删除旧表。
- 登录后公安局、公权机构、私权机构、公民、管理员、CPMS、账户、资料库均从结构化表读取。
- 文档已更新，中文注释已同步，旧残留已清理。

执行记录：
- 2026-06-04：完成后端结构化数据库重构，删除历史聚合目录和旧测试入口。
- 2026-06-04：完成 CPMS、公民、资料库、链端公开查询、管理员登录与安全动作的结构化表读写改造。
- 2026-06-04：更新 SFID 架构文档和并发框架说明。
- 2026-06-04：完成本地无用包装函数、空目录和旧符号扫描清理。
- 2026-06-04：修复登录挑战清理 SQL 的 PostgreSQL 参数类型推断问题,改为 Rust 计算截止时间后传入 SQL,并补充数据库错误明细输出。

验收结果：
- `cargo check` 通过。
- `cargo check --tests` 通过。
- `cargo test` 通过。
- `npm run build` 通过，Vite 仅提示前端产物体积较大。
- 二维码登录 challenge 接口实测返回 `200 OK`。
- SFID 代码、当前 SFID 架构文档和本任务卡未命中旧主数据符号。
- 后端不存在旧主数据目录、旧机构目录和空目录。

任务状态：
- 已完成。
