# 公民投票完整实现

## 状态：待开发

## 背景

联合投票提案进入公民投票阶段（stage=2）后，所有绑定了 SFID 的公民均可投票。当前 UI 框架已搭好，但提交逻辑和签名流程为 TODO 状态。

## 待完成项

### 1. SFID 绑定校验

- 公民投票按钮仅对"我的钱包中有绑定了 SFID 的钱包"的用户显示
- 我的钱包中没有绑定 SFID 的钱包的用户，进入公民投票阶段的提案详情页不显示投票按钮
- 校验方式：本地钱包地址 vs 链上 SFID 绑定记录

### 2. 公民投票签名流程

- 热钱包：wuminapp 本地直接签名
- 冷钱包：弹出 QR 码 → wumin 签名 → 扫回执
- 管理员（冷钱包）：同冷钱包流程，弹 QR 码 → wumin 签名

### 3. 公民投票提交逻辑

- `_submitCitizenVote` 当前为 TODO（仅提示"公民投票功能开发中"）
- 需要实现链上 `citizen_vote` extrinsic 的构造和提交
- 需要构建 WUMIN_SIGN_V1.0.0 协议的 display 字段（action=citizen_vote）

### 4. VoteChecker 公民投票支持

- `VoteChecker.fetchVote` 增加公民投票链上存储查询（CitizenVotesByAccount 或对应存储）
- 列表红点覆盖公民投票阶段（当前只覆盖内部投票和联合投票）

### 5. 列表红点

- 公民投票阶段的提案，如果用户有 SFID 绑定钱包且未投票，显示红点

## 已完成的基础设施

- [x] `ProposalContext` 公共层（role: admin/citizen/viewer）
- [x] `VoteChecker` 统一投票状态检查（kind=0 内部, kind=1 联合）
- [x] 公民投票 UI 框架（按钮、确认弹窗、进度展示）
- [x] `_citizenVoteOpen` 阶段判断
- [x] 两套协议定义（WUMIN_LOGIN_V1.0.0 / WUMIN_SIGN_V1.0.0）

## 涉及模块

- wuminapp（Mobile Agent）
- wumin（Mobile Agent）
- 区块链（Blockchain Agent）— citizen_vote extrinsic 确认
