# CPMS 档案详情有效期日期对齐

## 任务需求

- 档案详情页“有效期”显示为两行日期。
- 冒号与连字符单独成列上下对齐。
- 两行日期左侧对齐，避免 `-` 紧贴第二行日期。

## 预计修改目录

- `cpms/frontend/operator_admin`：调整档案详情页有效期 DOM 结构。
- `cpms/frontend/assets/styles`：调整有效期两行日期排版样式。
- `memory/08-tasks`：记录本次 UI 修复和验证结果。

## 执行清单

- [x] 创建任务卡。
- [x] 修改有效期显示结构。
- [x] 修改 CSS 对齐样式。
- [x] 运行前端构建和残留检查。

## 验收标准

- `：` 与 `-` 在同一列上下对齐。
- 起止日期在同一列上下对齐。
- 第二行不再显示为 `-2036年...` 这种紧贴样式。

## 完成记录

- 2026-05-30：档案详情页有效期改为标记列和日期列两列显示。
- 2026-05-30：`：` 与 `-` 单独对齐，日期列上下对齐，第二行不再紧贴连字符。
- 2026-05-30：`npm run build` 通过。
