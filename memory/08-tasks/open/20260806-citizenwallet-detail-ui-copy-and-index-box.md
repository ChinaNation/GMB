# 任务卡：公民钱包 钱包详情/账户详情 文案与序号方框整改

状态：代码完成(2026-08-06;dart format + analyze 零问题,受影响 3 个测试文件
11 用例全绿含新增序号徽章形态测试;残留复核干净——「SS58 地址」仅剩技术注释,
属官方编码术语按第一死规则保留)。待双端目视验收。

## 需求(用户已确认)

1. 账户详情:`公钥（账户 ID）`→`公钥（给电脑看的）`;`SS58 地址`→`账户地址（给人看的）`;
   括号及内文缩小一档并减淡(textTertiary),与主标签形成差异。代码标识符
   `ss58Address`/`accountId` 一律不动(Substrate 官方命名死规则,只改 UI 展示文案)。
2. 钱包码弹窗(单源 wallet_qr_dialog.dart,两入口共用):删除
   `钱包码：扫描可向本账户转账，或用于扫码登录`;底部改为 关闭|复制 左右对称双按钮。
   复制=复制账户地址(ss58Address),不关弹窗,提示「账户地址已复制」(用户已确认)。
3. 钱包详情:标题`账户`→`账户列表`;账户序号方框(38×38,上限 //1989)自适应:
   ≤99 保持 `#xx` 单行居中+FittedBox 兜底;≥100 `#` 缩小减淡定位左上角,
   数字另起一行居中并按位数自动缩小。
4. 助记词标签:`助记词（钱包备份，一句恢复全部账户）`→`助记词（钱包唯一备份，请绝对保密）`
   (只换文字,样式不动)。
5. 产品手册同步:密钥关系图 `SS58 地址`→`账户地址`(用户已确认)。

## 改动面

- lib/ui/account_detail_page.dart:_infoTile 主/副标签拆分;复制提示只用主标签。
- lib/ui/widgets/wallet_qr_dialog.dart:删说明行 + 关闭|复制 双按钮。
- lib/ui/wallet_detail_page.dart:账户列表标题、序号徽章自适应、助记词标签。
- lib/ui/product_manual_page.dart:SS58 地址→账户地址。
- 测试:account_detail_page_test / wallet_detail_page_test / product_manual_page_test
  断言锚点同步(弹窗锚点从被删文案换成 QrImageView+按钮);新增序号徽章 ≥100 形态测试。

## 边界

- 纯展示层:零链改动、零 QR 载荷改动(钱包码字节不变)、签名/派生不动。
- Flutter 单代码库,iOS/Android 双端一致;验收双端各跑一次。

## 验收

- [x] flutter analyze 零问题;受影响测试全绿(Isar 测试 --concurrency=1,11/11)。
- [ ] 双端真机/模拟器目视:账户详情标签、弹窗按钮对称、#1989 序号不溢出、助记词文案。
