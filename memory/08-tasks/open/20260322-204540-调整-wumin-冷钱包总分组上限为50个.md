# 调整 wumin 冷钱包总分组上限为 50 个

## 任务背景

用户希望将 `wumin` 冷钱包的分组上限从当前的 `20` 个总分组调整为 `50` 个总分组。

## 当前状态

- 分组管理页使用 `maxGroups = 20`
- 当前上限按“总分组数”计算，包含默认分组：
  - `全部`
  - `分组一`
  - `分组二`

## 风险点

- 若只修改常量不检查提示文案，用户提示可能与实际行为不一致
- 若后续仍按“还能新建多少个”理解，容易再次混淆“总分组数”和“额外可新建数”

## 执行计划

1. 将 `wumin` 分组管理页的总分组上限从 `20` 调整为 `50`
2. 保持上限提示文案与新值一致
3. 运行最小分析验证

## 实际处理

- 将 `wumin/lib/ui/group_management_page.dart` 中的 `maxGroups` 从 `20` 调整为 `50`
- 增加注释，明确该值表示“总分组数上限”，包含默认分组

## 验证结果

- 已运行 `dart analyze /Users/rhett/GMB/wumin/lib/ui/group_management_page.dart`
- 当前仅剩 1 条既有告警：`_deleteGroup` 未被引用
- 该告警与本次上限调整无直接关系
