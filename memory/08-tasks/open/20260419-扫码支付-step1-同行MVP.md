# 扫码支付清算体系 · Step 1 · 同清算行内 MVP

- **日期**:2026-04-19
- **状态**:open / in-progress
- **ADR**:`memory/04-decisions/ADR-006-扫码支付-step1-同行MVP.md`
- **取代**:`memory/08-tasks/open/20260405-offchain-deposit-model-redesign.md`(标 superseded)

## 进度快照(2026-04-19)

- ✅ **Runtime 层全部完成**:`batch_item.rs` / `bank_check.rs` / `nonce.rs` / `deposit.rs` / `lib.rs` Storage+Event+Error+Call / `institution-asset-guard` 扩展 / `configs/mod.rs` PowTxAmountExtractor + `DuoqianSfidAccountQuery` / tests Test 补 `type SfidAccountQuery`
- ✅ **Node 层骨架完成**:`node/src/offchain/` 下 5 个文件就位(mod/ledger/rpc/packer/event_listener),main.rs 挂载,旧 3 个 `offchain_*.rs` 头注释标 deprecated
- ✅ **SFID 系统完成**:核查发现 `is_clearing_bank` + 双账户机制已存在,本步只补 `app_search_clearing_banks` 公开搜索端点,路由 `GET /api/v1/app/clearing-banks/search`(技术文档 `memory/05-modules/sfid/backend/institutions/CLEARING_BANK_SEARCH_API.md`)
- ✅ **wuminapp 完成**:新增 8 文件(3 RPC + 5 页面)+ 改造 2 文件(wallet_page 加菜单项 + bind_clearing_page 加 deprecated)。`flutter analyze` 零错误(技术文档 `memory/05-modules/wuminapp/offchain/STEP1_PAGES.md`)
- ✅ **Step 2a(Runtime 重写)完成**:新增 3 子模块(`fee_config` / `solvency` / `settlement`)+ `OffchainBatchItemV2` + 3 Storage(L2FeeRateBp / L2FeeRateProposed / MaxL2FeeRateBp) + 3 Call(`submit_offchain_batch_v2` call_index 34,`propose_l2_fee_rate` 40,`set_max_l2_fee_rate` 41) + `SfidAccountQuery::is_admin_of` + `on_initialize` 激活到期费率。runtime configs `DuoqianSfidAccountQuery` 补 `is_admin_of`,`PowTxAmountExtractor` 分类 3 新 call。与旧"省储行清算"代码**共存**(技术文档 `STEP2A_RUNTIME.md`)
- ✅ **Step 2b-i(节点业务逻辑)完成**:`offchain/ledger.rs` 加 `NodePaymentIntent` + `accept_payment`(L3 sr25519 验签 + nonce + 余额 + pending 入账)+ `reject_pending` + `take_pending_for_batch`;`offchain/rpc.rs` 加 `offchain_submitPayment` + `SubmitPaymentResp`;`offchain/mod.rs` 加 `OffchainComponents` + `start_clearing_bank_components`。`OffchainBatchItemV2` 补 `DecodeWithMemTracking` 派生以满足 runtime Call 解码。offchain-transaction-pos 和 node/offchain/ 子树零编译错误(技术文档 `STEP2B_I_NODE.md`)
- ✅ **Step 2b-ii-α(packer 业务逻辑 + 依赖注入)完成**:`offchain/packer.rs` 新增 `BatchSigner` / `BatchSubmitter` 两个 trait + `NoopBatchSigner` / `NoopBatchSubmitter` 占位 + `NodeBatchItem`(字段与 runtime `OffchainBatchItemV2` 逐字节对齐)+ `From<PendingPayment>` + `batch_signing_message` 纯函数(`blake2b_256(DOMAIN‖institution‖batch_seq_le‖batch_bytes)`);重写 `OffchainPacker`:持有 signer/submitter/batch_seq_counter,`pack_and_submit` 完整流程(取 pending → 转 items → 递增 seq → 签名 → 提交 → 失败回滚 / 成功保留等事件);`mod.rs` 启动器签名增补,追加 `start_clearing_bank_components_with_noop` 便捷入口;新增 4 个 tokio + 2 个同步单测(noop 回滚 / happy path / submitter err / 签名消息确定性 / 编码长度 268 字节)。offchain/ 子树零编译错误(技术文档 `STEP2B_II_A_PACKER.md`)
- ✅ **Step 2b-ii-β-1(真实 signer)完成**:新建 `offchain/keystore_signer.rs` 实现 `KeystoreBatchSigner`,从 `offchain_keystore::SigningKey` 派生 sr25519 签名;`Arc<RwLock<Option<SigningKey>>>` 支持热切换密钥;5 个单测(验签通过 / 未加载 Err / 不同消息 / 错误密钥不验过 / 热切换)。`offchain/mod.rs` 挂载新子模块并 `pub use KeystoreBatchSigner` 供 β-2 service.rs 用。offchain/ 子树零编译错误零警告(技术文档 `STEP2B_II_B_1_SIGNER.md`)
- ✅ **Step 2b-ii-β-2-a(extrinsic 构造器)完成**:新建 `offchain/pool_submitter.rs` 实现 `PoolBatchSubmitter`(持 `Arc<FullClient>` + `Arc<TxPool>` + signing_key);纯函数 `build_signed_extrinsic` 严格对齐 `benchmarking.rs::create_benchmark_extrinsic` 的 12 元 `TxExtension` 顺序;纯函数 `decode_batch_items` / `encode_bounded_sig` / `encode_bounded_batch`。`submit` 本步用 `nonce=0 + 占位 hash`(β-2-b 补真实 `account_nonce` 查询和 `pool.submit_one` 调用);TxPool 类型别名绑定 `runtime::opaque::Block`(非 `runtime::Block`)以匹配 `service.rs` 泛型约束。node Cargo.toml 新增 `offchain-transaction-pos` / `frame-support` / `frame-system-rpc-runtime-api` 三个依赖。5 个单测(batch 字节 roundtrip / bounded sig/batch 容量 / 非法字节拒绝 / 密钥 None 语义)。offchain/ 子树零错误(技术文档 `STEP2B_II_B_2_A_SUBMITTER.md`)
- ✅ **Step 2b-ii-β-2-b(substrate 集成)完成**:`pool_submitter.submit` 替换 β-2-a 占位为真实 `pool.submit_one` + `account_nonce` 查询;`cli.rs` 加 `--clearing-bank <SS58>` 与 `--clearing-bank-password <STR>` 两个 flag;`service.rs` 的 `new_full` 签名加 2 个参数,在 `transaction_pool` 构造完成后启动清算行组件(解锁 keystore → 组装 signer/submitter → `start_clearing_bank_components` → `task_manager.spawn` 30 秒 tick 的 packer loop);`command.rs` 与 `ui/node_runner.rs` 透传 CLI 参数;`rpc.rs::FullDeps` 加 `offchain_clearing_rpc` 字段,`create_full` 合并 `OffchainClearingRpcServer::into_rpc`;`OffchainClearingRpcImpl` 派生 `Clone`。清算行节点端到端链路打通(wuminapp RPC → ledger.accept_payment → packer.pack_and_submit → PoolBatchSubmitter → TransactionPool → settlement::execute_clearing_bank_batch)。offchain / service / rpc / cli / command / node_runner 全部零编译错误(技术文档 `STEP2B_II_B_2_B_INTEGRATION.md`)
- ✅ **Step 2b-iii-a(event_listener 真实订阅)完成**:`offchain/event_listener.rs` 新增 `run(self: Arc<Self>, client: Arc<FullClient>)` async 长循环,订阅 `import_notification_stream`(非 finality,以降低扫码支付延迟,runtime 防重放保 reorg 安全)→ 仅处理 `is_new_best` block → `process_block` 读 `System::Events` storage(`twox_128("System") ++ twox_128("Events")`)→ SCALE decode `Vec<EventRecord<runtime::RuntimeEvent, H256>>` → `convert_event` 过滤 `OffchainTransactionPos` 5 个变体(`Deposited` / `Withdrawn` / `PaymentSettled` / `BankBound` / `BankSwitched`,`transfer_amount→amount` + `fee_amount→fee` 字段映射)→ `handle` 分发到 ledger 清理 pending 和同步余额。`service.rs` 清算行启动块追加 `offchain-clearing-event-listener` spawn task(与 packer task 并列)。顺带修复 Step 2b-ii-α 引入的 `packer.rs` 测试对 `ledger.inner` 私有字段访问:`LedgerInner` 升 `pub(super)` + 3 字段 + `OffchainLedger.inner` 同步,并加注释说明"仅限同目录测试注入"。新增 3 个 `convert_event` 单测(正路径 / 字段映射 / 非本 pallet 返 None),保留原 3 个 handle 单测。闭环打通:wuminapp RPC → ledger → packer → pool → runtime 事件 → listener → ledger.pending 清理(技术文档 `STEP2B_III_A_EVENT_LISTENER.md`)
- ✅ **Step 2b-iii-b(reserve_monitor 主账对账)完成**:新建 `offchain/reserve_monitor.rs`(`ReserveMonitor` 结构 + `run(client, interval)` 长循环 + `check_once` 单次对账 + `bank_total_deposits_key(bank)` storage key 构造);`ledger.rs` 加只读 `confirmed_sum_snapshot() -> u128`(不含 pending_debit/credit —— pending 期间链上本地同时保"未扣"天然相等);`mod.rs::OffchainComponents` 追加 `reserve_monitor` 字段并在 `start_clearing_bank_components` 构造;`cli.rs` 加 `--clearing-reserve-monitor-interval-secs`(缺省 300s,0 关闭);`service.rs::new_full` 签名再加 1 个参数,清算行启动块内 event_listener spawn 之后追加 `offchain-clearing-reserve-monitor` spawn(interval=0 时打 warning 并跳过);`command.rs` + `ui/node_runner.rs` 同步透传(UI 路径 None,None,None)。5 个单测(confirmed_sum 空/累加/withdraw + storage key 布局稳定/不同 bank 不同 key)。节点 / 节点测试全部零 error(技术文档 `STEP2B_III_B_RESERVE_MONITOR.md`)
- ✅ **Step 2b-iv-a(节点侧省储行清算代码清理)完成**:删除 3 个旧文件 `offchain_{ledger,packer,gossip}.rs`(合计 736 行);`main.rs` 删 3 个 mod 声明;`service.rs` 删 `static OFFCHAIN_CONFIG` / `struct OffchainConfig` / `set_offchain_config` / `get_offchain_config` / 老 gossip 协议注册 / channel 创建 / `rpc_extensions_builder` 三字段 / `offchain-clearing-gossip` spawn(约 50 行);`rpc.rs` 从 800 行减到 422 行(删 3 个 RPC `offchain_submitSignedTx/queryTxStatus/queryInstitutionRate` + 2 helper `query_institution_rate_bp/calc_offchain_fee` + `FullDeps` 3 字段 + 未用 import);`ui/home/process/mod.rs` 删 `RuntimeState.offchain_ledger` + `start_node_sync` 老 ledger 切入块 + `stop_node_sync` pending 保护(~58 行,UI 不启动清算行组件时本就是死代码);`offchain_keystore.rs` 保留但头注释净化(明确是节点统一密钥容器,`shenfen_id` 字段名留 Step 3 rename);`offchain/mod.rs` + `offchain/ledger.rs` 头注释更新为当前实际运行状态描述。wuminapp `onchain_trade_page.dart` 下架老入口(扫码后 SnackBar 提示,不再跳转到 `offchain_pay_page`)。`cargo check -p node --tests` 零 error;`flutter analyze` 零 issue(技术文档 `STEP2B_IV_A_CLEANUP.md`)
- ✅ **Step 2c-i(wuminapp 付款端新版)完成**:节点侧 `offchain/rpc.rs` 新增 2 个只读 RPC(`offchain_queryUserBank(user)` 读链上 `UserBank[user]`;`offchain_queryFeeRate(bank)` 读 `L2FeeRateBp[bank]` 返回 `{ rate_bp, min_fee_fen=1 }`),`OffchainClearingRpcImpl` 加 `client: Arc<FullClient>` 字段;`offchain/mod.rs::start_clearing_bank_components` + `service.rs::new_full` 调用处透传 `client`。wuminapp 新建 `lib/trade/offchain/payment_intent.dart`(`NodePaymentIntent` 数据类 + 定长 204 字节 SCALE 编码 + `GMB_L3_PAY_V1` 域前缀 + blake2_256 signingHash + 四舍五入 `calcFeeFen` + `Random.secure` tx_id + hex 编解码)+ `offchain_clearing_pay_page.dart`(5 阶段状态机:loading → ready → submitting → done/error,流程:queryUserBank → SFID 解析收款方 shenfen_id → 同行校验 → queryFeeRate → fetchLatestBlock(expires_at=+100) → queryNextNonce → scaleEncode → signingHash → walletManager.signWithWalletNoAuth → submitPayment 提交 → 显示 tx_id);`lib/rpc/offchain_clearing.dart` 加 `queryUserBank` / `queryFeeRate` / `submitPayment` 3 方法。`onchain_trade_page._openOffchainPay` 恢复扫码后跳转到新页面,读 `SFID_BASE_URL` / `CLEARING_NODE_WSS` env 占位。**删除**老 `lib/rpc/offchain.dart` + `lib/trade/offchain/offchain_pay_page.dart`。同行 MVP 端到端:wuminapp 扫码 → 构造 intent → 热钱包签名 → RPC 提交 → 节点 ledger.accept_payment → packer 30s tick 上链 → settlement → 事件 → listener 清 pending → reserve_monitor 对账。`cargo check -p node --tests` 零 error;`flutter analyze` 零 issue(技术文档 `STEP2C_I_PAY_PAGE.md`)
- ✅ **Step 2c-ii-a(wuminapp 收款端基础)完成**:新建 `lib/trade/offchain/clearing_bank_prefs.dart`(`SharedPreferences` 封装,按 `walletIndex` 隔离存取用户绑定清算行 `shenfen_id`;key=`clearing_bank_shenfen_id_{walletIndex}`;save 空串等价 clear)+ 5 个单测全绿(空 load / 双 index roundtrip / 空串清除 / 选择性 clear / 覆盖写入)。新建 `lib/trade/offchain/offchain_clearing_receive_page.dart`:入参 `WalletProfile` + 可选 `clearingNodeWssUrl`;init 读 prefs → 未绑定提示,已绑定渲染 `WUMIN_QR_V1 kind=user_transfer` 二维码(`address/name/amount/memo/bank=shenfen_id` 五字段);若 wssUrl 给了,每 5 秒 `offchain_queryBalance` 轮询刷新余额,`_balanceInFlight` 防重入,失败原地 banner 不 crash。`bind_clearing_bank_page` 链上绑定提交后立即 `ClearingBankPrefs.save(walletIndex, sfidId)`(解决链上 `UserBank` 只存主账户无 `shenfen_id` 的反向映射问题;Step 3 SFID 加 by-main-account 反查 API 后移除本地缓存)。`clearing_payment_entry_page` 追加"生成收款码"入口;提示文本同步更新。老 `lib/wallet/ui/receive_qr_page.dart` 并存不动(旧省储行路径),Step 2b-iv-b runtime 清理时再下架。`flutter analyze` 全项目零 issue(技术文档 `STEP2C_II_A_RECEIVE_QR.md`)
- ✅ **E2E 冒烟 · Layer A(PaymentIntent 跨端 golden vectors)完成**:Rust `batch_item.rs::tests` 新增 3 组 golden fixture(简单同行 / 跨行 u128/u64/u32 max / 零金额 + tx_id 递增),锁 `encoded` 204 字节 hex + `signing_hash` 32 字节 hex 期望值。Dart `test/trade/payment_intent_golden_test.dart` 新建 5 个测试(3 组同 fixture 同期望 hex + 签名域 `GMB_L3_PAY_V1` 13 字节 ASCII + 编码长度定长 204)。任一端 SCALE 布局 / 字节序 / 签名域 / blake2 hash 漂移都会两端 CI 同时红。**顺带修复** `offchain-transaction-pos/src/lib.rs:2406` 测试 mock `voting_engine_system::Config for Test` 缺 `type MaxAdminsPerInstitution`(补 `ConstU32<32>`),pallet 测试二进制重新可编译。修复后暴露 3 个**预存在**业务测试失败(`tests::enqueue_offchain_batch_validates_admin_and_signature` / `rate_update_requires_internal_vote_pass` / `submit_batch_executes_real_settlement_and_marks_processed`),不属于本步范围已 spawn_task 登记独立修复。`cargo test -p offchain-transaction-pos --lib batch_item::tests` 5 ok;`flutter test payment_intent_golden_test.dart` 5 ok;`flutter analyze` 零 issue(技术文档 `STEP2C_GOLDEN_VECTORS.md`)
- ✅ **E2E 冒烟 · 手工 SOP 指南完成**:落地 `memory/05-modules/wuminapp/offchain/STEP2C_MANUAL_SMOKE.md`,12 个章节(前置 / 节点启动 / wuminapp 启动 / 两钱包准备 / 绑定 / 充值 / 生成收款码 / 扫码付款 / 最终状态验证 / 故障排查 / checklist / 后续)。每步给出**期望日志关键字**与**期望链上/本地状态**两路观察点;第 9 节覆盖 8 个常见故障(绑定状态、SFID 搜索未中、跨行拦截、费率未配置、keystore 密码错、nonce 冲突、余额刷新、对账偏差)。定位:假设清算行机构 / 费率 / 主账户等由运维 / SFID Agent 事先准备(明确 out-of-scope),SOP 聚焦 wuminapp ↔ 清算行节点 ↔ runtime 的扫码付款/收款交互验证。也作为 Layer B 自动化测试的 case 清单源(技术文档 `STEP2C_MANUAL_SMOKE.md`)
- ✅ **A · 修复 3 个 pallet 预存在业务测试完成**:根因为 Step 2c-ii-a 修好 `voting_engine_system::Config for Test` 缺 `MaxAdminsPerInstitution` 编译后暴露的**运行时**失败 —— `TestInternalAdminProvider` 仅实现了 `is_internal_admin`,未覆盖 Default `get_admin_list` → `AdminSnapshot` 提案创建时不落 → 投票 `vote_institution_rate` 走 `is_admin_in_snapshot` 返回 false → `NoPermission`。修复:为 `TestInternalAdminProvider` 新增 `get_admin_list(org, institution)` 实现,返回 `CHINA_CH[].duoqian_admins` 内置管理员 + `EXTRA_TEST_ADMINS` 追加管理员合并列表(与 `is_internal_admin` 判定逻辑一致)。`cargo test -p offchain-transaction-pos --lib` 从 20 ok + 3 fail → **23 ok,0 fail**(5 个 golden + 旧基础 + 3 个之前预存在失败全部 pass)。runtime 主逻辑零改动
- ✅ **B · Step 2c-iii 冷钱包扫签完成**:`lib/trade/offchain/offchain_clearing_pay_page.dart` 把签名步骤抽成 `_signSigningHash(signingHash, amountFen, feeFen)`。热钱包(`isHotWallet`)继续走 `WalletManager.signWithWalletNoAuth`;冷钱包走 `QrSigner.buildRequest(payloadHex=hex(signingHash), display={action:'offchain_clearing_pay', summary, 5 fields})` → `encodeRequest` → `QrSignSessionPage` 两段 QR 握手,返回 `SignResponseEnvelope` → 取 `body.signature` hex → hexToBytes → 64 字节签名 → `offchain_submitPayment`。复用 `bind_clearing_page.dart` 同款模式,**wumin 冷钱包 app 零改动**(冷钱包对 32 字节 signing_hash 盲签,与 `NodePaymentIntent` SCALE 布局解耦)。删除 Step 2c-i 的 `!isHotWallet` 拒绝短路。`flutter analyze` 零 issue(技术文档 `STEP2C_III_COLD_WALLET.md`)
- ✅ **C · Step 2b-iv-b runtime 老省储行代码彻底清除完成**:**物理删除**老省储行清算体系全部代码,约 **2900+ 行**:(1) pallet `lib.rs` 从 2873 行精简到 434 行 —— 删 13 个老 Call(`submit_offchain_batch` / `enqueue_offchain_batch` / `process_queued_batch` / `bind_clearing_institution` / `propose_institution_rate` / `vote_institution_rate` / 一组 prune/skip/cancel/retry)+ 14 个老 Storage(`InstitutionRateBp` / `LastBatchSeq` / `QueuedBatches` / `RateProposalActions` / `RecipientClearingInstitution` / 一组 prune cursor / 一组摘要)+ 17 个老 Events + ~20 个老 Errors + 全部 helper + `ProtectedSourceChecker` trait + `RateProposalAction`/`BatchSummary`/`QueuedBatchRecord` types + 老 `on_idle` 清理钩子 + 老 `GenesisConfig.initial_rates`;保留新体系全部 Storage/Calls(30-34/40/41)+ `ProcessedOffchainTx/At`(settlement 仍用)(2) `benchmarks.rs` 74→13 行(删 3 个老 benchmark)(3) `weights.rs` 55→17 行(`WeightInfo` 简化为空壳)(4) `configs/mod.rs::PowTxAmountExtractor` 删 5 处老分支 + `RuntimeFeePayerExtractor` 简化为只对 `submit_offchain_batch_v2` 识别;`ProtectedSourceChecker` impl + Config 两个关联 type 同步去掉(5) node UI `mod.rs` 删 4 个 Tauri 命令 + `handler!` 注册;`signing.rs` 删 2 个签名构造函数(~160 行);`proposal.rs` 删 `fetch_rate_proposal_action` + `fetch_institution_rate_bp` + `resolve_proposal_action` 的费率分支(6) `spec_version: 8 → 9`,`storage_version: 1 → 2`。dev 链用 `developer_direct_upgrade` setCode 升级,**不做** `on_runtime_upgrade` migration(用户授权直接 dev 升级,老 Storage 物理字节随 chain data 生命周期自然消亡)。`cargo test -p offchain-transaction-pos --lib` 10 ok(5 golden + 2 signing_hash + 3 bank_check);`cargo check -p node --tests` 零 error(仅 Tauri);`flutter analyze` 零 issue(技术文档 `STEP2B_IV_B_RUNTIME_CLEANUP.md`)
- ✅ **D · Layer B pallet 集成测试完成**:新建 `runtime/transaction/offchain-transaction-pos/src/tests.rs`(562 行)+ `lib.rs` 追加 `#[cfg(test)] mod tests;`。构造 mock runtime(`frame_system + pallet_balances + OffchainPos` + `MockSfid` SfidAccountQuery + `()` InstitutionAssetGuard fail-open),预置清算行主/费用账户 + 唯一管理员 + L3 用户(sr25519 `Pair::from_seed` 派生,公钥直接作 AccountId32)。8 个新测试:绑定/存取/提现全流程、重复绑定拒绝、未注册清算行拒绝、切换前置校验(2 个)、`submit_offchain_batch_v2` 非管理员拒绝、同行批次端到端(10 000 分转账 5 bp fee:DepositBalance/BankTotalDeposits/pallet_balances 三层状态全部校验 + PaymentSettled/ClearingBankBatchSettled 事件断言 + nonce=2 重签 tx 撞 TxAlreadyProcessed)、签名过期拒绝。`cargo test -p offchain-transaction-pos --lib` **从 10 ok → 20 ok**。不做:dev chain 启动(Layer C)、wuminapp Flutter E2E(技术文档 `STEP2_D_LAYER_B_PALLET_INTEGRATION.md`)
- ✅ **E · 跨行 ghost account bug 修复完成**:`node/src/offchain/ledger.rs::on_payment_settled` 签名从 5 参扩到 8 参(增 `payer_bank` / `recipient_bank` / `my_bank`),内部改为"按本行归属分支动账":`payer_bank == my_bank` 才动 payer 状态,`recipient_bank == my_bank` 才动 recipient(新建或累加);跨行场景下**不再给不在本行的 recipient 建 ghost 账户**,修复 `confirmed_sum_snapshot` 与链上 `BankTotalDeposits[my_bank]` 虚高的 P1 bug(Step 1 同行 MVP 不触发,Step 3 跨行启用前必修)。`event_listener.rs::handle` 调用点同步 `self.my_bank` 注入。ledger 测试由 1 个 settle 用例扩到 4 个:同行(原升级)+ `settled_cross_bank_payer_side_only_no_ghost_recipient`(跨行 payer 本行,recipient 不出现在 accounts) + `settled_cross_bank_recipient_side_only`(跨行 recipient 本行,payer 历史状态不误扣)+ `settled_sum_invariant_same_bank_drops_by_fee`(对账不变式:Σ confirmed 变化 == -fee)。`cargo check -p node --tests` 零 error(仅 Tauri);pallet 20 ok 不受影响;关联 Step 2b-iii-a 登记的 spawn_task 可关闭(技术文档 `STEP2_E_CROSS_BANK_GHOST_FIX.md`)
- ✅ **F · 全仓库老省储行代码/注释/文档残留扫描 + 彻底清除完成**:A-E 完成后做仓库级 grep 扫描,清掉 6 处活引用:(1) **wuminapp** — `lib/rpc/onchain.dart` 删 `_bindClearingInstitutionCallIndex` / `bindClearingInstitution` / `queryClearingInstitution` / `_buildBindClearingCall` + 相关 hash 工具;`lib/wallet/ui/wallet_page.dart` 删 `_boundClearingBankId` / `_loadClearingBank` / `_openBindClearingBank` / `_openReceiveQr` 方法 + 对应 PopupMenuItem(`'receive'` / `'clearing'`);删除 `lib/wallet/ui/bind_clearing_page.dart` + `lib/wallet/ui/receive_qr_page.dart` + `lib/trade/offchain/clearing_banks.dart`(43 省储行硬编码)+ `lib/governance/fee_rate_detail_page.dart`;`lib/governance/transfer_proposal_service.dart` 删 `submitVoteRate` / `_buildVoteRateCall` / `_offchainPalletIndex` / `_voteRateCallIndex` / `fetchRateProposalAction` / `FeeRateProposalInfo` 类 + `ProposalWithDetail.feeRateDetail` 字段 + 相关调用分支;`lib/governance/institution_detail_page.dart` + `lib/governance/all_proposals_view.dart` 删 FeeRateDetailPage import + 导航分支。(2) **node frontend TS** — `api.ts` 删 `buildRateVoteRequest` / `queryInstitutionRateBp` / `buildProposeRateRequest` / `submitProposeRate`;`governance/GovernanceSection.tsx` 删 `'propose-fee-rate'` route + `onCreateFeeRate` 回调 + `FeeRateProposalPage` import 与渲染;`governance/InstitutionDetailPage.tsx` 删 "费率设置" 按钮 + `onCreateFeeRate` prop;`governance/VoteSigningFlow.tsx` 删 `useRateVote` prop + `buildRateVoteCallDataHex` 函数;`governance/ProposalDetailPage.tsx` 删 "费率设置详情" 区块 + `useRateVote` 传参;`governance/governance-types.ts` 删 `FeeRateProposalDetail` 类型 + `ProposalFullInfo.feeRateDetail` 字段;删除 `governance/FeeRateProposalPage.tsx`。(3) **wumin(冷钱包)** — `signer/action_labels.dart` 删 `propose_institution_rate` / `vote_institution_rate` 映射,加回新 `bind_clearing_bank` / `offchain_clearing_pay` 中文 label;`signer/payload_decoder.dart` 删 `_decodeBindClearingInstitution` / `_decodeProposeInstitutionRate` / `_decodeOffchainPay` 三个解码分支;`signer/offline_sign_service.dart` + `qr/offline_sign_page.dart` 的 allowedHashedActions 移除 3 个老 action;`signer/pallet_registry.dart` 替换 4 个老 callIndex 常量为新清算行 7 个常量(30/31/32/33/34/40/41)。(4) **过期文档** — `memory/05-modules/citizenchain/runtime/transaction/offchain-transaction-pos/FEE_RATE_GOVERNANCE.md` → `FEE_RATE_GOVERNANCE_LEGACY.md` + 顶部红字 deprecation banner;`OFFCHAIN_TECHNICAL.md` → `OFFCHAIN_TECHNICAL_LEGACY.md` + 同款 banner;`memory/05-modules/wuminapp/governance/GOVERNANCE_TECHNICAL.md` 更新 4.4 节 从"省储行业务治理"改为"清算行(L2)费率治理",5.1 节删除已下线的 vote 列表。(5) **历史注释保留** — 所有源码内"Step 2b-iv-b 已删除"的历史说明性注释(约 10 处)保留作为变更记录。验证:pallet 20/20 ok、`cargo check -p node --tests` 零 error(仅 Tauri 门禁)、`flutter analyze`(wuminapp + wumin)零 issue、`tsc --noEmit`(node frontend)exit=0。全仓库再次 grep 老符号只剩显式"已下线/LEGACY"历史注释
- ✅ **G · X1-UI 省储行签名管理员 UI 功能彻底删除 + ChinaCh 两死字段清除完成**:(1) **前端 TS** — `api.ts` 删 `setSigningAdmin` / `getSigningAdmin` + `SigningAdminInfo` import;`governance-types.ts` 删 `SigningAdminInfo` interface;`AdminListPage.tsx` 删 `signingAdmin` state / `isSigningAdminKey` 计算 / "设为验证者" 按钮 + "已设为验证者" 标签 / 整个验证者弹窗(~80 行)+ `submitValidator` 方法 + 5 个相关 useState(2) **Rust Tauri** — **整文件删除** `node/src/ui/settings/cold-wallets/mod.rs`(168 行含 `SigningAdminInfo` struct / `save_signing_admin_meta` / `load_signing_admin_meta` / `find_province_for_admin` / `set_signing_admin` / `get_signing_admin` 2 个 Tauri 命令)+ 删整个 `cold-wallets/` 目录;`ui/settings/mod.rs` 删 `cold_wallets` sub-module + path 声明;`ui/mod.rs::generate_handler!` 删 2 行注册(3) **Rust lib 保留** — `offchain_keystore.rs` 整体保留(清算行节点 CLI 启动仍需);头注释净化改为"清算行(L2)管理员密钥容器",明确老"省储行签名管理员 UI 功能"已清除,本模块仅作为 Rust lib 供 CLI 路径调用,未来清算行 Tab 新 UI 将复用 `save_signing_key` / `load_signing_key` 公开 API;`SigningKey.shenfen_id` 字段名保留以避免 blast radius,留 Step 3 rename(4) **`primitives/china/china_ch.rs` ChinaCh 死字段清除** — 删除 `pub enabled: bool`(注释说"前端禁止选择"但全代码面零读取)+ `pub wss_url: &'static str`(原供老 wuminapp `offchain_submitSignedTx` 走 WSS 到省储行节点,Step 2b-iv-a 删 `offchain.dart` 后成死字段);43 条记录各删 2 行赋值,**净删 ~90 行**;struct 从 11 字段精简到 9 字段。**删除工具**:用 `perl -i -ne 'print unless /^\s*(wss_url\|enabled)\s*:/'` 一次删完 43 条,保留 struct 定义下的历史说明注释(5) **验证全绿** — `cargo check --workspace --exclude node` 零 error;`cargo test -p offchain-transaction-pos --lib` 20/20 ok;`cargo check -p node` 仅 Tauri 门禁;`flutter analyze`(wuminapp + wumin)No issues;`tsc --noEmit`(frontend)exit=0。全仓库 grep 老 signing admin 符号只剩 `offchain_keystore.rs` 保留的 lib 使用 + 历史说明注释,无任何错位置 UI 引用
- ✅ **编译验证**:`offchain-transaction-pos`(Step 1 + Step 2a)+ `institution-asset-guard` + `node` + `sfid-backend` + `wuminapp` 全部零编译错误
- ✅ **技术文档**:`STEP1_TECHNICAL.md` / `OFFCHAIN_NODE_STEP1.md` / `CLEARING_BANK_SEARCH_API.md` / `STEP1_PAGES.md` / `STEP2A_RUNTIME.md`
- ⏳ **Step 2b(Node 接入)**:`packer.pack_and_submit` 实现、`ledger.accept_payment`、`gossip.rs`、`service.rs`/`rpc.rs` 接入、删除旧 3 节点文件
- ⏳ **Step 2c(wuminapp 重写)**:`offchain_pay_page` / `receive_qr_page` 重写 + 冷钱包 QR 签名路径
- ⏳ **Step 2d(清理)**:删除 pallet 内旧 `submit_offchain_batch` / `bind_clearing_institution` / `RecipientClearingInstitution` / `InstitutionRateBp`
- ⏳ **集成测试 / 压测**:Step 2b/2c 完成后做

---

## 任务目标

在现有 `offchain-transaction-pos` pallet 内新增模块,实现:

1. 清算行身份判定(A3 ∈ {SFR, FFR} + duoqian Active)
2. L3 绑定 / 开户 / 存取 / 切换清算行
3. L3 PaymentIntent 签名结构与 nonce 管理
4. 同清算行内扫码支付的 execute 分账(**Step 1 仅 enable bind/deposit/withdraw/switch,同行清算逻辑作为内部函数就绪,供 Step 2 启用批次上链**)

本步**不触碰**现有 `submit_offchain_batch / execute_batch / RecipientClearingInstitution / InstitutionRateBp` 等省储行清算模型,保持共存。

## 范围(做)

| 项 | 位置 |
|---|---|
| ADR | memory/04-decisions/ADR-006-扫码支付-step1-同行MVP.md ✓ |
| 新增 batch_item.rs / bank_check.rs / nonce.rs / deposit.rs | citizenchain/runtime/transaction/offchain-transaction-pos/src/ |
| lib.rs 新增 4 个 Storage + 5 个 Event + 10 个 Error + 4 个 Call | 同上 |
| institution-asset-guard 新增 4 个 Action | citizenchain/runtime/transaction/institution-asset-guard/src/ |
| configs/mod.rs PowTxAmountExtractor 分类 | citizenchain/runtime/src/configs/mod.rs |
| 新目录 node/src/offchain/ + 5 个文件 | citizenchain/node/src/offchain/ |
| SFID 数据库迁移 + generator 扩展 + search API | sfid/backend/ |
| wuminapp 6 新文件 + 3 改造文件 | wuminapp/lib/offchain/、wuminapp/lib/wallet/ |
| STEP1_TECHNICAL.md 技术文档 | memory/05-modules/ |

## 范围(不做,留 Step 2/3)

- 跨清算行扫码支付
- `L2FeeRateBp` Storage + 费率治理(Step 1 硬编码 5 bp 写在 lib.rs 常量)
- 偿付能力自动保护
- `dispute` / `reserve` / `close_clearing_bank`
- 清算行间 libp2p gossip
- 删除旧 `offchain_ledger.rs` / `offchain_packer.rs` / `offchain_gossip.rs`(本步仅标 deprecated)
- 重写现有 execute_batch

## 子任务拆分

### 第 1 周:Runtime(Blockchain Agent)

- T1-1:`batch_item.rs`(PaymentIntent 结构 + 签名哈希)
- T1-2:`bank_check.rs`(A3 + Active 判定 + fee_account_of)
- T1-3:`nonce.rs`(L3PaymentNonce consume)
- T1-4:`deposit.rs`(bind/deposit/withdraw/switch 四函数)
- T1-5:`lib.rs` 新增 Storage(UserBank / DepositBalance / BankTotalDeposits / L3PaymentNonce)
- T1-6:`lib.rs` 新增 Events / Errors / Calls(call_index 30-33)
- T1-7:`institution-asset-guard` 新增 4 个 Action + 默认实现
- T1-8:`configs/mod.rs` PowTxAmountExtractor 分类 4 个新 call
- T1-9:单元测试(bind/deposit/withdraw/switch 正反路径)

### 第 2 周:Node(Blockchain Agent)

- T2-1:`node/src/offchain/mod.rs` 启动器
- T2-2:`node/src/offchain/ledger.rs` 本地账本(AES-256-GCM 加密)
- T2-3:`node/src/offchain/rpc.rs` offchain_* 方法 + WS 推送
- T2-4:`node/src/offchain/packer.rs`(骨架,Step 2 启用)
- T2-5:`node/src/offchain/event_listener.rs` 监听 Deposited/Withdrawn
- T2-6:`service.rs` 按清算行角色启动;旧 `offchain_*.rs` 标 `#[deprecated]`
- T2-7:`rpc.rs` 注册 offchain 命名空间

### 第 3 周:SFID(SFID Agent)+ wuminapp 前半(Mobile Agent)

- T3-1:`sfid/backend/migrations/` 新增 `business_type` 列
- T3-2:`sfid/backend/src/sfid/generator.rs` 扩展清算行注册(两账户派生)
- T3-3:`sfid/backend/src/public/institutions.rs` + 路由 `GET /public/institutions/search`
- T3-4:wuminapp `lib/offchain/sfid_public_api.dart`
- T3-5:wuminapp `lib/offchain/offchain_rpc_client.dart`
- T3-6:wuminapp `lib/offchain/clearing_bank_list_page.dart`
- T3-7:wuminapp `lib/offchain/bind_clearing_bank_page.dart`

### 第 4 周:wuminapp 后半 + 集成(Mobile Agent + 主入口)

- T4-1:wuminapp `lib/offchain/deposit_page.dart`
- T4-2:wuminapp `lib/offchain/withdraw_page.dart`
- T4-3:wuminapp `lib/wallet/wallet_detail_page.dart` 改造
- T4-4:wuminapp `lib/offchain/offchain_pay_page.dart` 改造(同行扫码)
- T4-5:wuminapp `lib/offchain/receive_qr_page.dart` 改造(bank 字段)
- T4-6:集成测试 9 用例
- T4-7:压测 60 万笔/10 分钟
- T4-8:`STEP1_TECHNICAL.md` + 白皮书修订草案回写

## 验收标准

- [ ] `offchain-transaction-pos` 新增代码单测全过
- [ ] 旧 `submit_offchain_batch` 路径仍可工作(未破坏)
- [ ] 清算行两步注册 → 两账户 Active
- [ ] L3 绑定 / 充值 / 提现 / 切换全流程链上事件正确
- [ ] 同行扫码支付 A 秒级成功 + B 收 WS 推送
- [ ] 跨行扫码支付返回 `CrossBankNotSupportedInMvp`
- [ ] wuminapp 未绑定清算行时扫码有引导,无回落链上
- [ ] 二维码严格遵循 `WUMIN_QR_V1 + user_transfer`
- [ ] 集成 9 用例通过
- [ ] 压测 60 万笔成功率 100%
- [ ] `memory/05-modules/citizenchain/runtime/transaction/offchain-transaction-pos/STEP1_TECHNICAL.md` 回写

## 风险

- 清算行节点单点 → 文档指导主备(生产等 Step 2)
- L3 未绑定体验 → wuminapp 首次扫码前就引导
- 批次上链失败堆积 → 复用现有 `QueuedBatches` 队列(Step 2 启用)
- 节点 ledger 与链上漂移 → event_listener 强一致

## 关联

- ADR:`memory/04-decisions/ADR-006-扫码支付-step1-同行MVP.md`
- 二维码规范:`memory/05-architecture/qr-protocol-spec.md`
- 白皮书:5.4.2、2.5、6.3(Step 3 修订 + 新增 5.4.4)

## 变更记录

- 2026-04-19:创建,承接过去 20 轮讨论的完整需求定稿。
