# 任务卡：统一本地调试时 sfid 后端监听地址与手机访问地址，修复提交升级提案时人口快照接口连接超时问题

- 任务编号：20260321-084304
- 状态：open
- 所属模块：sfid,wuminapp
- 当前负责人：Codex
- 创建时间：2026-03-21 08:43:04

## 任务需求

统一本地调试时 sfid 后端监听地址与手机访问地址，修复提交升级提案时人口快照接口连接超时问题

## 必读上下文

- memory/00-vision/project-goal.md
- memory/00-vision/trust-boundary.md
- memory/01-architecture/repo-map.md
- memory/03-security/security-rules.md
- memory/07-ai/agent-rules.md
- memory/07-ai/context-loading-order.md
- <补充该模块对应技术文档路径>

## 必须遵守

- 不可突破模块边界
- 不可绕过既有契约
- 不可擅自修改安全红线
- 不清楚逻辑时先沟通
- 改代码后必须更新文档和清理残留

## 输出物

- 代码
- 中文注释
- 文档更新
- 残留清理

## 待确认问题

- 暂无

## 实施记录

- 任务卡已创建
- 已将 `sfid/backend` 监听地址改为读取 `SFID_BIND_ADDR`，避免后端固定绑定 `127.0.0.1:8899`
- 已将 `sfid/start-dev.sh` 健康检查与端口探测改为复用 `SFID_BIND_ADDR`
- 已将 `wuminapp/scripts/app-run.sh` 与 `wuminapp/scripts/app-clean-run.sh` 改为优先读取 `sfid/.env.dev.local` 中的 `SFID_BIND_ADDR` 生成 `WUMINAPP_API_BASE_URL`
- 已更新 `sfid/.env.dev.local` 本地开发地址为 `147.224.14.117:8899`，使后端监听地址与手机访问地址保持一致
- 已同步更新 `memory/05-modules/sfid/backend/chain/CHAIN_TECHNICAL.md` 与 `memory/05-modules/wuminapp/wallet/WALLET_TECHNICAL.md`
- 已完成验证：`cargo check --manifest-path sfid/backend/Cargo.toml`、`bash -n sfid/start-dev.sh`、`bash -n wuminapp/scripts/app-run.sh`、`bash -n wuminapp/scripts/app-clean-run.sh`
- 修正联调语义：后端监听地址改为 `0.0.0.0:8899`，手机访问地址改为独立的 `SFID_PUBLIC_BASE_URL=http://10.92.152.128:8899`，避免把不可绑定的外部地址误用为本机监听地址
- 已将创世哈希策略调整为：开发环境通过 `SFID_CHAIN_RPC_URL` 启动时自动获取，生产环境通过代码内 `TRUSTED_PRODUCTION_CHAINS` 白名单校验链身份
- 已将 `sfid/start-dev.sh` 调整为启动前主动清理占用 `SFID_BIND_ADDR` 端口的旧 backend 进程，确保 `cd /Users/rhett/GMB/sfid && ./start-dev.sh` 启动的是最新后端实现
- 已修复 `wuminapp` 提案/投票签名链路中的管理员钱包一致性约束：
  - `WalletManager.signWithWallet()` 现会在热钱包签名前校验 seed 派生公钥与当前钱包 `pubkeyHex` 一致
  - `QrSigner.parseResponse()` / `QrSignSessionPage` 现会在冷钱包回执阶段校验回执 `pubkey` 与页面选中钱包一致
  - `runtime_upgrade_page.dart`、`transfer_proposal_page.dart`、`transfer_proposal_detail_page.dart`、`onchain_trade_page.dart` 已接入 `expectedPubkey`
- 已同步更新 `memory/05-modules/wuminapp/wallet/WALLET_TECHNICAL.md`、`memory/05-modules/wuminapp/governance/GOVERNANCE_TECHNICAL.md` 与 `memory/05-modules/wuminapp/qr/QR_TECHNICAL.md`
- 已完成验证：`/Users/rhett/flutter/bin/cache/dart-sdk/bin/dart format ...`、`flutter analyze ...`（仅剩项目既有 warning，无新增错误）
- 已定位当前升级提案失败的真正根因：`sfid` 链上信任主公钥是 `0x14e4...1607`，但本地数据库 `runtime_meta` 仍恢复出旧活动主公钥 `0xc262...f85c`，导致人口快照签名链上验签失败，联合提案创建报 `JointVoteCreateFailed`
- 已将 `sfid` 主签名人模型调整为：
  - 链上 `SfidMainAccount/SfidBackupAccount1/SfidBackupAccount2` 是三把受信任公钥的唯一真相
  - `sfid` 本地只保留当前主私钥 `SFID_SIGNING_SEED_HEX`
  - 备用私钥不再进入 `sfid`
- 已移除 `runtime_meta` 对活动主私钥/主公钥/已知 seed 映射的恢复能力，避免数据库旧状态覆盖 `.env`
- 已新增启动硬校验：
  - 启动时通过链 RPC 同步链上三把公钥到本地 `chain_keyring_state`
  - 本地主私钥派生公钥必须等于链上 `main` 公钥，否则 `sfid` 直接拒绝启动签名服务
- 已将 `key-admins` 本地 keyring 初始镜像改为只接受备用公钥，不再接受备用 seed 进入 `sfid`
- 已同步更新 `memory/05-modules/sfid/backend/key-admins/KEY_ADMINS_TECHNICAL.md` 与 `memory/05-modules/sfid/backend/chain/CHAIN_TECHNICAL.md`
- 已完成验证：`cargo check --manifest-path /Users/rhett/GMB/sfid/backend/Cargo.toml`
- 已完成验证：`cargo test --manifest-path /Users/rhett/GMB/sfid/backend/Cargo.toml validate_active_main_signer_with_keyring_rejects_runtime_mismatch -- --nocapture`
- 已完成验证：`cargo test --manifest-path /Users/rhett/GMB/sfid/backend/Cargo.toml sensitive_seed_debug_remains_redacted -- --nocapture`
- 已进一步定位并修复一个更底层的主因：`sfid` 之前用裸 `schnorrkel::MiniSecretKey::expand_to_keypair(Uniform)` 从 `SFID_SIGNING_SEED_HEX` 派生主公钥，这与链 runtime 实际使用的 Substrate `sp-core::sr25519` 派生规则不一致；同一条 seed 会被 `sfid` 算成错误公钥，导致“seed 正确但人口快照签名链上仍验不过”
- 已将 `sfid/backend` 中所有“从 seed 派生主公钥 / 用主私钥签名”的入口统一切换到 `sp-core::sr25519`
- 已补充回归测试：开发链当前测试 seed `0xb642...f4da` 必须派生成链创世主公钥 `0x14e4...1607`
- 已完成验证：`cargo check --offline --manifest-path /Users/rhett/GMB/sfid/backend/Cargo.toml`
- 已完成验证：`cargo test --offline --manifest-path /Users/rhett/GMB/sfid/backend/Cargo.toml substrate_seed_derivation_matches_dev_chain_main_pubkey -- --nocapture`
- 已完成验证：`cargo test --offline --manifest-path /Users/rhett/GMB/sfid/backend/Cargo.toml validate_active_main_signer_with_keyring_rejects_runtime_mismatch -- --nocapture`
- 本轮启动联调时发现当前机器上的开发链 RPC（`127.0.0.1:9944` / `10.92.152.128:9944`）此刻不可连；因此尚未完成“新后端进程成功连链并监听”的最终运行态验证，但签名派生与启动硬校验代码路径已通过编译和针对性测试
