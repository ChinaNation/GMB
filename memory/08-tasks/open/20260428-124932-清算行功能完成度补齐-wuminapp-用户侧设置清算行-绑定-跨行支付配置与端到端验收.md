# 任务卡：清算行功能完成度补齐：wuminapp 用户侧设置清算行、绑定、跨行支付配置与端到端验收

- 任务编号：20260428-124932
- 状态：open
- 所属模块：wuminapp
- 当前负责人：Codex
- 创建时间：2026-04-28 12:49:32

## 任务需求

清算行功能完成度补齐：wuminapp 用户侧设置清算行、绑定、跨行支付配置与端到端验收

## 必读上下文

- memory/00-vision/project-goal.md
- memory/00-vision/trust-boundary.md
- memory/01-architecture/repo-map.md
- memory/03-security/security-rules.md
- memory/07-ai/agent-rules.md
- memory/07-ai/context-loading-order.md
- wuminapp/WUMINAPP_TECHNICAL.md

## 模块模板

- 模板来源：memory/08-tasks/templates/wuminapp.md

### 默认改动范围

- `wuminapp`

### 先沟通条件

- 修改 Isar 数据结构
- 修改认证流程
- 修改关键交互路径


## 模块执行清单

- 清单来源：memory/07-ai/module-checklists/wuminapp.md

# WuMinApp 模块执行清单

- App 只是交互入口，不承担信任根职责
- Isar 结构、认证流程、关键交互变化前必须先沟通
- 关键 Flutter 交互与本地存储逻辑必须补中文注释
- 文档与残留必须一起收口


## 模块级完成标准

- 标准来源：memory/07-ai/module-definition-of-done/wuminapp.md

# WuMinApp 完成标准

- App 仍然只是交互入口
- 关键 Flutter 交互和 Isar 逻辑已补中文注释
- 文档已同步更新
- 关键交互或数据结构变化已先沟通
- 残留已清理


## 必须遵守

- 不可突破模块边界
- 不可绕过既有契约
- 不可擅自修改安全红线
- 不清楚逻辑时先沟通
- 改代码后必须更新文档和清理残留

## 输出物

- 代码
- 中文注释
- 文档更新
- 残留清理

## 本卡目标

- 把 wuminapp 侧完成度从约 55% 补到可验收状态。
- 范围包括 `wuminapp/lib/trade/offchain`、`wuminapp/lib/rpc`、钱包详情入口、清算行设置页、绑定/切换/充值/提现/扫码付款流程与测试文档。

## 待补齐清单

- 将 `ClearingBankSettingsPage` 从占位页改为真实搜索、列表、当前绑定状态、绑定 / 切换入口。
- 清算行节点地址不能只依赖启动参数；需要从 SFID / 链上 `ClearingBankNodes` 结果得到可用端点或提供明确配置入口。
- 移动端付款页与 runtime 的收款方主导模型对齐：明确同行、跨行各自是否放开，若放开跨行则补付款方 / 收款方清算行展示与确认。
- 绑定后本地 `ClearingBankPrefs` 与链上 `UserBank` 的一致性检查和失效恢复。
- 补齐用户端端到端验收：搜索清算行、绑定、充值、生成收款码、扫码付款、提现。

## 验收标准

- `flutter analyze` 通过。
- 清算行相关 Flutter 测试通过，并新增设置页真实交互测试。
- 端到端手工 SOP 更新并跑通一次。
- App 仍只是交互入口，不新增信任根或保存链上权威状态。

## 待确认问题

- 暂无

## 实施记录

- 任务卡已创建
- 2026-04-29 Codex 执行：
  - 新增 `lib/rpc/clearing_bank_directory.dart`,通过 SFID 后端搜索清算行候选,并从链上
    `OffchainTransaction::ClearingBankNodes` / `UserBank` 读取端点与用户绑定。
  - `ClearingBankSettingsPage` 从占位页改为真实搜索、当前绑定、绑定/切换入口。
  - `ClearingBankPrefs` 从单一 sfid 字符串升级为 `ClearingBankBindingSnapshot`,缓存机构名称、主/费用账户、peerId、rpcDomain/rpcPort。
  - 绑定页支持 bind/switch 两种模式,成功后写入完整 snapshot。
  - 钱包卡接入清算行余额查询、充值、提现入口;未绑定时明确提示先去“清算行”页面绑定。
  - 扫码付款按收款方 `bank` 查询链上清算节点端点,提交到收款方清算节点;付款页放开跨行并展示付款方/收款方清算行与清算类型。
  - 充值/提现页面接入本地绑定检查,避免无清算行节点配置时误提交。

## 本轮验证

- 通过：`flutter analyze`（wuminapp）
- 通过：`flutter test test/wallet/ui/cards/wallet_action_card_test.dart test/trade/clearing_bank_prefs_test.dart test/trade/clearing_bank_settings_page_test.dart`（12 passed）

## 本轮剩余项

- 端到端手工 SOP 尚未在真实 dev 链跑完：搜索清算行、绑定、充值、生成收款码、跨行扫码付款、提现。
- 历史记录沉淀仍待后续接 `PaymentSettled` 事件或本地交易记录体系。
