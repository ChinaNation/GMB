# CitizenConsole 发币控制台查看与布局统一

## 任务目标

1. 已配置的发币地址与 `SETTLE_TOKEN` 补齐「查看」操作，其余已有查看逻辑保持一致。
2. 订单台账右侧按上下两行放置解锁状态和解锁/锁定按钮。
3. 页面统一使用「发币控制台」「订单台账」「配置列表」。

## 2026-08-03 二次收敛目标

1. 订单台账标题左侧只显示解锁状态圆点：未解锁红色、已解锁绿色，不显示状态文字。
2. 操作按钮固定为“刷新台账、拉取并发币、解锁/锁定、配置”一行；解锁为橙色、锁定为
   红色、配置为灰蓝色。
3. 删除页面下方独立配置区域，把原配置列表及全部操作收进“配置”按钮打开的专用弹窗。
4. 关闭配置弹窗必须立即清理已展开输入和短时查看值，但不得锁定持续发币会话；页面主体
   只保留订单台账和订单列表。

## 2026-08-03 三次收敛目标

1. 删除页面中的“订单台账”标题，四个操作按钮按原顺序靠左，红/绿解锁状态圆点独立靠右。
2. 订单表在“订单”前增加“序号”列，按现有 `updated_at` 倒序结果从 1 开始跨页连续编号；序号只是展示信息，不持久化。
3. 前端固定每页 20 条，订单列表右下角显示上一页、当前页/总页数、下一页；刷新台账或发币后回到第一页。

## 安全边界

- 所有查看动作逐次要求 Touch ID，不复用发币控制台的持续解锁状态。
- `SETTLE_TOKEN` 只允许在本机页面行内短时显示，不写日志、不缓存、不落盘。
- 发币地址只由原生安全进程读取发币私钥并派生；普通 Node 和浏览器只能取得公开地址。
- 发币私钥 `DISBURSE_KEY` 永不返回普通 Node、浏览器或日志。
- 不改变持续解锁、锁定、订单结算、发币、订阅或 runtime 逻辑。
- 不推送 GitHub，不触发远端 CI。

## 实施步骤

- [x] 补齐 `SETTLE_TOKEN` 与发币地址查看能力。
- [x] 调整订单台账右侧解锁状态和按钮布局。
- [x] 统一三项页面文案并清理残留。
- [x] 补充安全与页面结构测试。
- [x] 更新安全文档和相关任务记录。
- [x] 重建签名应用并完成真实页面验收。
- [x] 二次收敛订单台账状态与四按钮布局。
- [x] 把配置列表迁入专用弹窗并清理短时敏感页面状态。
- [x] 更新测试、文档并清理旧布局残留。
- [ ] 重建签名应用并完成二次真实页面验收。
- [x] 完成顶部左按钮/右状态布局、跨页序号与每页 20 条分页。
- [x] 更新测试、架构文档并清理“订单台账”可见标题及旧布局残留。
- [x] 把解锁状态圆点从右侧居中改为订单区域右上角绝对定位。

## 预计修改目录

- `citizenconsole/web/`：修改发币控制台页面结构、文案、按钮渲染及局部样式。
- `citizenconsole/topup/`：扩展固定配置查看接口，但禁止返回发币私钥。
- `citizenconsole/security-app/`：增加逐次 Touch ID 的公开发币地址派生操作。
- `citizenconsole/server.mjs`：把固定原生地址派生操作注入充值发币路由。
- `citizenconsole/test/`：补充查看安全、页面布局和文案测试。
- `memory/03-security/`：更新发币控制台安全边界和统一名称。
- `memory/08-tasks/`：记录任务执行、验收和旧文案残留清理。

本次二次收敛只修改以下既有目录，不新增文件：

- `citizenconsole/web/`：修改订单台账结构、状态圆点、四按钮布局、配置弹窗和中文注释；
  涉及前端代码与旧独立配置区域残留清理。
- `citizenconsole/test/`：更新页面结构、按钮顺序、弹窗敏感状态清理和单一事件入口测试；
  只涉及测试代码。
- `memory/03-security/`：把配置短时查看的页面边界更新为专用弹窗；只涉及安全文档。
- `memory/08-tasks/open/`：记录本轮确认、实施和真实验收证据；只涉及现有任务卡。

本次三次收敛只修改以下既有目录，不新增文件：

- `citizenconsole/web/`：调整顶部左/右布局、序号列、每页 20 条的前端分页和中文注释；涉及前端代码与旧标题布局残留清理。
- `citizenconsole/test/`：覆盖按钮顺序、状态位置、跨页连续序号、空台账和分页边界；只涉及测试代码。
- `memory/01-architecture/gmb/`：同步发币控制台当前页面结构和分页规则；只涉及架构文档。
- `memory/08-tasks/open/`：记录确认范围、实施和验收证据；只涉及本现有任务卡。

## 执行记录

- 2026-08-02：用户确认执行；创建任务卡，当前分支为 `main`。
- 2026-08-02：`SETTLE_TOKEN` 已恢复逐次 Touch ID 查看；发币地址经新增的原生
  `topup.address` 固定操作派生，只把公开地址返回普通 Node。已解锁与未解锁两种状态下
  查看都逐次要求 Touch ID，查看动作不改变持续发币会话。
- 2026-08-02：订单台账顶部已改为两列两行，右上显示解锁状态、右下显示解锁/锁定按钮；
  页面标题统一为「发币控制台」「订单台账」「配置列表」，旧布局选择器和相关有效文档
  残留已清理。
- 2026-08-02：`npm test` 59 项全部通过；`npm run check`、ShellCheck 和
  `git diff --check` 通过。
- 2026-08-02：通过 CitizenConsole 自身的 Touch ID「编译」动作重建正式签名包，
  `start.sh verify` 以及包内 `server.mjs`、充值路由与三个网页资源和源码逐字节比对通过。
- 2026-08-02：真实签名应用页面验收通过。页面显示「发币控制台」「订单台账」「配置列表」；
  解锁状态与标题纵向相差 1px，解锁按钮和刷新台账按钮纵坐标完全一致。展开代币子项后，
  9 个已配置实际值均有「查看」。发币地址与 `SETTLE_TOKEN` 分别逐次通过 Touch ID，均以
  只读行显示并带 20 秒自动隐藏；验收未复制或输出令牌值，结束后明文行已清空。
- 2026-08-03：用户确认二次收敛方案；保留“拉取并发币”文案，按钮颜色固定为解锁橙色、
  锁定红色、配置灰蓝色。本轮不修改后端、原生安全程序、充值结算、订阅或 runtime，不推送
  GitHub、不触发远端 CI 或部署。
- 2026-08-03：源码已完成订单台账单主体、左侧红/绿状态圆点、四按钮单行和配置专用弹窗；
  关闭按钮、ESC、配置重绘与代币分组折叠均走同一临时值清理函数，立即清空输入、逐次查看值
  及计时闭包，不调用锁定接口。CitizenConsole 59 项测试、完整 `npm run check`、ShellCheck
  和 `git diff --check` 全部通过，旧两列两行选择器与页面下方配置区域已清理。
- 2026-08-03：现有生产签名包完整性校验通过，但三个网页资源与本轮源码不同，仍待换包。
  已在可见 Chrome 触发唯一 Touch ID 打开流程，系统正常进入等待状态，随后因未收到本机指纹
  返回“生物验证未通过”并关闭；未使用密码或绕过生物识别。因此签名换包与真实页面验收继续
  保持未完成，待本机完成一次 Touch ID 后继续。
- 2026-08-03：用户确认三次收敛方案：每页固定 20 条，序号按现有最新优先排序跨页连续生成；分页仅在前端处理，不修改台账存储、结算接口、Touch ID、订阅或 runtime。
- 2026-08-03：三次收敛实施完成。页面已删除“订单台账”可见标题，四按钮组靠左，红/绿状态圆点靠右；序号列在订单列之前，按每页 20 条跨页连续计算。分页区使用自动上边距固定在订单页面右下角，空台账两个翻页按钮均禁用。
- 2026-08-03：Node、Bash、Swift 检查及 CitizenConsole 59 项测试全部通过；架构文档、中文注释与旧布局残留已同步清理。最新源码已重建、签名、原子换包并通过 `start.sh verify`，签名包内三个网页资源与源码逐字节一致，`next/` 无残留。
- 2026-08-03：真实签名页面分别在内置浏览器和可见 Chrome 发起了官方 Touch ID 打开流程；内置页面持续等待本机指纹，Chrome 返回“生物验证未通过”并失败关闭。未使用密码或绕过生物识别，因此本轮不冒充报告已完成真实视觉验收。
- 2026-08-03：用户进一步明确状态圆点必须显示在右上角，确认以订单区域为定位容器，圆点距顶部和右侧各 16px；按钮组预留右侧空间，不修改任何业务或安全逻辑。
- 2026-08-03：右上角定位实施完成。`.tp-ledger-section` 作为唯一相对定位容器，`.tp-session-dot` 固定为 `top: 16px; right: 16px`，并删除旧 `margin-left: auto` 居中布局；按钮行右侧预留 27px，避免与圆点重叠。Node、Bash、Swift 检查和 59 项测试全部通过，最新签名包已重建、原子换包并通过完整性校验。
