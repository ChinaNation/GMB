# 任务卡：按业务边界重新设计并落地 SFID 省管理员相关前后端与 runtime 目录结构

- 任务编号：20260502-114447
- 状态：open
- 所属模块：sfid
- 当前负责人：Codex
- 创建时间：2026-05-02 11:44:47

## 任务需求

按业务边界重新设计并落地 SFID 省管理员相关前后端与 runtime 目录结构

## 必读上下文

- memory/00-vision/project-goal.md
- memory/00-vision/trust-boundary.md
- memory/01-architecture/repo-map.md
- memory/03-security/security-rules.md
- memory/07-ai/agent-rules.md
- memory/07-ai/context-loading-order.md
- sfid/README.md
- sfid/SFID_TECHNICAL.md

## 模块模板

- 模板来源：memory/08-tasks/templates/sfid-backend.md

### 默认改动范围

- `sfid/backend`
- 必要时联动 `sfid/deploy`

### 先沟通条件

- 修改 permit 模型
- 修改账户绑定规则
- 修改数据库结构


## 模块执行清单

- 清单来源：memory/07-ai/module-checklists/sfid.md

# SFID 模块执行清单

- 不保存原始实名
- permit、绑定、数据库结构变化前必须先沟通
- 关键接口和数据模型必须补中文注释
- 文档与残留必须一起收口


## 模块级完成标准

- 标准来源：memory/07-ai/module-definition-of-done/sfid.md

# SFID 完成标准

- 仍然满足 SFID 不保存原始实名
- 关键接口、数据模型与边界判断已补中文注释
- 文档已同步更新
- permit、绑定、数据库结构变化已先沟通
- 残留已清理


## 必须遵守

- 不可突破模块边界
- 不可绕过既有契约
- 不可擅自修改安全红线
- 不清楚逻辑时先沟通
- 改代码后必须更新文档和清理残留

## 输出物

- 代码
- 中文注释
- 文档更新
- 残留清理

## 待确认问题

- 暂无

## 实施记录

- 任务卡已创建
- 已将省管理员 main 公钥、Slot、ProvinceAdmins 等从 `sfid/backend/src/sfid/province.rs`
  拆入 `sfid/backend/src/sheng_admins/province_admins.rs`;`province.rs` 只保留 SFID 省市代码。
- 已将省管理员签名 seed 持久化从 `store_shards/sheng_signer.rs` 移入
  `sfid/backend/src/sheng_admins/signing_seed_store.rs`。
- 已将后端链交互目录 `chain/sheng_admin/`、`chain/sheng_signer/`、`chain/sheng_pending/`
  合并为 `sfid/backend/src/chain/sheng_admins/`。
- 已删除前端 `sfid/frontend/src/` 与 `src/views/`,把页面迁到根层业务目录。
- 已新增前端 `sfid/frontend/chain/sheng_admins/`,承载省管理员链交互页面、API 和类型。
- 已将前端 `chain/duoqian-info/` 统一为 `chain/duoqian_info/`,与后端/runtime 命名对齐。
- 已新增 runtime `citizenchain/runtime/otherpallet/sfid-system/src/sheng_admins/`,
  承载 Slot 类型、payload domain/hash 和迁移提示。
- 已处理二次残留:
  - 清除本地数据库中旧 `KEY_ADMIN` 会话缓存、`admins` 残留行、`v_key_admins`
    旧视图与 `key_admin_keyring` 旧空表。
  - 修正 `sfid/backend/db/migrations/014_drop_key_admins.sql`,确保迁移先删除
    旧视图和旧 keyring 表,再清理旧角色行,避免外键/视图依赖导致迁移失败。
  - 收紧 `admins_role_check`,只允许 `SHENG_ADMIN` / `SHI_ADMIN`。
  - 移除省管理员签名 seed 持久化对旧 `SFID_SHENG_SIGNER_DIR` 环境变量的兼容 fallback。
  - 更新活跃技术文档中的省管理员权限口径,不再把旧全局管理员作为有效角色描述。
- 已处理前端白屏:
  - 根因不是 5179 端口,而是 Vite dev 原生 ESM 加载完整依赖图时未完成入口模块执行,
    浏览器停留在 `#root` 空状态,终端无编译错误。
  - 已打断 `App.tsx -> 业务视图 -> App.tsx` 的共享样式循环依赖,新增
    `sfid/frontend/common/cardStyles.ts` 承载 `glassCardStyle` / `glassCardHeadStyle`。
  - 已将 `npm run dev` 调整为 `npm run build && vite preview --host 127.0.0.1 --port 5179`,
    用构建预览模式稳定打开页面;原始 Vite serve 保留为 `npm run dev:raw` 供后续单独排查。
- 已更新文档:
  - `memory/01-architecture/repo-map.md`
  - `memory/05-modules/sfid/backend/chain/CHAIN_TECHNICAL.md`
  - `memory/05-modules/sfid/frontend/FRONTEND_LAYOUT.md`
  - `memory/05-modules/sfid/backend/sheng_admins/SHENG_ADMINS_TECHNICAL.md`
  - `memory/05-modules/citizenchain/runtime/otherpallet/sfid-system/SFID_SYSTEM_TECHNICAL.md`

## 验证记录

- `rg` 残留扫描:
  - 前端无 `../src`、`src/views`、`api/sheng_admin`、`api/sheng_signer`、`types/slot` 运行时代码引用。
  - 后端无 `chain/sheng_admin/`、`chain/sheng_signer/`、`chain/sheng_pending/` 运行时代码引用。
  - runtime 省管理员 Slot / payload 已位于 `src/sheng_admins/`, `lib.rs` 仅 re-export 兼容测试与外部引用。
- `cd sfid/backend && cargo check`:通过,仅保留 `sfid/province.rs` 行政区层级字段的既有 dead_code warning。
- `cd sfid/frontend && npm run build`:通过,Vite 仅提示现有 chunk 体积与动态/静态重复 import warning。
- `cd citizenchain/runtime/otherpallet/sfid-system && cargo check`:通过,仅有依赖 future-incompat warning。
- 本地数据库最终验证:
  - `runtime_cache_entries` 中 `KEY_ADMIN` 命中数为 0。
  - `runtime_misc` 中 `KEY_ADMIN` 命中数为 0。
  - `admins` 中 `KEY_ADMIN` 行数为 0。
  - `v_key_admins` 与 `key_admin_keyring` 均不存在。
  - `admins_role_check` 为 `role IN ('SHENG_ADMIN', 'SHI_ADMIN')`。
- `psql -f sfid/backend/db/migrations/014_drop_key_admins.sql`:通过;迁移可在旧对象不存在时重复执行并保持上述结果。
- `cd sfid/frontend && npm run dev`:通过;5179 渲染管理员扫码登录页,浏览器 console 无 error/warn。
