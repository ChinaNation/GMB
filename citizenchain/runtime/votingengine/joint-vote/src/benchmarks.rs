//! Benchmarks for `joint-vote` pallet。
//!
//! cast_admin / cast_referendum / finalize_joint_timeout /
//! finalize_jointreferendum_timeout 的权重 benchmark 待 substrate-benchmark-cli
//! 完成真实跑测后填充,届时覆盖 `weights.rs` 的手工保守上界。
#![cfg(feature = "runtime-benchmarks")]
