//! Benchmarks for `internal-vote` pallet。
//!
//! 当前为占位骨架,等 substrate-benchmark-cli 跑测后再生成 cast /
//! finalize_internal_timeout 的精确权重。weight estimate 在 [`crate::weights`]
//! 为手工保守上界。
#![cfg(feature = "runtime-benchmarks")]
