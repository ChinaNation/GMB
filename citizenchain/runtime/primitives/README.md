# Runtime Primitives 目标目录

本目录用于承载 CitizenChain runtime 内部的常量、基础类型和运行时组织层内容。

说明：

- 当前仓库根目录已经存在 `primitives/` Rust crate
- 本目录是 CitizenChain 目标结构中的 runtime 内部 `primitives` 目录
- 后续是否将仓库根目录 `primitives/` 迁入本目录，需要在正式迁移阶段统一处理
