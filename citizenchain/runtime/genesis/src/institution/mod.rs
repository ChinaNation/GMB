//! 创世机构播种入口。
//!
//! - `fixed_roles`：五类固定创世岗位、席位及既有钱包索引映射，不写 storage。
//! - `seeder`：唯一链上写入方，写机构、岗位、任职和管理员钱包集合。
//!
//! 法定代表人不是创世必填项。创世阶段三字段允许全部为空；后续依法任命由
//! entity 运行期流程原子写入，创世不得从管理员钱包或机构主账户推导法定代表人。

mod fixed_roles;
mod seeder;

pub use seeder::build;
